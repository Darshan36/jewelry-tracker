# Known Gaps and Deferred Items

Living document of decisions deferred, caveats accepted, and items to revisit. Audited every 3–4 phases.

## How to use this file

- Items added when a phase intentionally defers work
- Each item has a clear revisit trigger (next phase / next major bump / when condition X happens)
- When an item is resolved, mark it `[resolved on YYYY-MM-DD]` and leave it in place for one audit cycle, then remove

## Decision lineage

Decisions where we deviated from an original spec or assumption, kept for institutional memory.

- **shadcn preset is `radix-nova`, not "default + slate".** The setup spec specified shadcn's classic "Default style + Slate base color." The shadcn CLI (≥4.x) replaced `--style` and `--base-color` with a preset list (Nova / Vega / Maia / Lyra / Mira / Luma / Sera / Custom); none are named "slate." We ran `shadcn init -b radix --css-variables -y` and got the `radix-nova` preset. The visual tokens it shipped are fully overridden in `src/app/globals.css`, so the preset choice is cosmetic-only — but any future `npx shadcn add <component>` will pull components written for the radix-nova variant.

- **Tailwind v4 with CSS-based `@theme`, not `tailwind.config.ts`.** The setup spec said "update `tailwind.config.ts`," but `create-next-app` now scaffolds Tailwind v4, which puts theme tokens in `globals.css` via the `@theme` directive. No `tailwind.config.ts` file exists. End result is the same — design tokens compile to Tailwind utilities and CSS variables.

- **Prisma 7 architecture: schema has no datasource URLs; connection split between `prisma.config.ts` (CLI/migrations) and an adapter on `PrismaClient` (runtime).** The Phase 1.5 spec assumed Prisma 6's pattern of `url = env("DATABASE_URL")` and `directUrl = env("DIRECT_URL")` inside `schema.prisma`. Prisma 7 (installed v7.8.0) rejects both fields in the schema (error P1012). The new architecture: (a) `prisma.config.ts` declares `datasource.url = process.env.DIRECT_URL` for the CLI / migrations / studio (session pooler, port 5432, supports prepared statements); (b) `src/lib/prisma.ts` constructs a `@prisma/adapter-pg` adapter with `process.env.DATABASE_URL` (transaction pooler, port 6543 + `?pgbouncer=true&connection_limit=1`) and passes it to `new PrismaClient({ adapter })` for runtime queries. The two-URL pattern is preserved — just plumbed through two different files instead of one schema block. Adapter dependencies added: `@prisma/adapter-pg`, `pg`, `@types/pg`.

- **Prisma 7 client barrel.** Prisma 7's `prisma-client` generator emits the client into `src/generated/prisma/` as separate files (`client.ts`, `models.ts`, `enums.ts`, etc.) with no `index.ts`. To make `import { PrismaClient } from '@/generated/prisma'` resolve canonically (the idiom that tutorials and AI assistants assume), `scripts/post-prisma-generate.mjs` writes a barrel that re-exports from `./client`. The `prisma:generate` npm script chains generation + barrel; `postinstall` delegates to it so fresh clones always have a working barrel.

- **Tailwind v4 spacing namespace collision — named `--spacing-*` tokens NOT exposed via `@theme`.** DESIGN.md defines spacing as `xs/sm/md/lg/xl = 4/8/16/24/40px`, but Tailwind v4's size-keyword utilities (`max-w-md`, `max-w-lg`, `max-h-xl`, `min-w-sm`, etc.) read from the same `--spacing-*` CSS variable namespace. Defining `--spacing-md: 16px` in `@theme` would shadow Tailwind's default size-keyword resolution and make `max-w-md` = 16px instead of 28rem. Discovered during Phase 1.6 Task 5 when the login card rendered ~200px wide instead of ~450px. Resolution: we use Tailwind's **numeric** spacing utilities exclusively (`p-1` = 4px, `p-2` = 8px, `p-4` = 16px, `p-6` = 24px, `p-10` = 40px) — these derive from the singular `--spacing` base unit (`0.25rem`) and don't collide. Equivalents table is documented in `src/app/globals.css`.

- **Auth.js v5 module augmentation requires `@auth/core/*` paths, not just `next-auth/*`.** Auth.js v5's docs show augmenting `next-auth` and `next-auth/jwt` to add custom fields (e.g. `Session.user.role`, `JWT.role`). In beta.31 reality, the `next-auth/jwt.d.ts` file is `export * from "@auth/core/jwt"` — TypeScript interface merging doesn't propagate through the `export *` re-export. Augmentation must target both the wrapper paths (`next-auth`, `next-auth/jwt`) AND the originals (`@auth/core/types`, `@auth/core/jwt`). See `src/types/next-auth.d.ts` for the four-module declaration. Discovered Phase 1.6 Task 3 — initial spec-shaped augmentation produced TS2322 errors (`token.id` typed as `unknown`).

- **Next.js 16 middleware → proxy rename.** The original Phase 1.6 spec called for `src/middleware.ts`; Next.js 16 deprecated that convention in favor of `src/proxy.ts`. File contents are unchanged (the `auth((req) => …)` wrapper from Auth.js works identically). Critically, **`proxy.ts` defaults to the Node.js runtime** per the official docs, which solves the Auth.js + Prisma edge-bundling problem (the proxy import chain pulls in `@prisma/adapter-pg` → `pg` → `node:net` etc.) without needing to split auth config into `auth.config.ts` + `auth.ts`. Future contributors looking at Auth.js v5 docs will see the split-config pattern; **ignore it** — our stack doesn't need it. Discovered Phase 1.6 Task 4 when the dev server failed to compile middleware.ts under Edge Runtime.

- **Public schema grants revoked from `anon` and `authenticated` roles.** Supabase's default setup grants table-level privileges on `public` to the `anon` (browser-exposed via `NEXT_PUBLIC_SUPABASE_ANON_KEY`) and `authenticated` roles. We don't use Supabase's REST/PostgREST endpoint at all — Prisma talks directly to Postgres through the pooler with the `postgres` role. So those grants were pure attack surface with zero benefit. Phase 1.5 ran:
  ```sql
  REVOKE ALL ON SCHEMA public FROM anon, authenticated;
  REVOKE ALL ON ALL TABLES IN SCHEMA public FROM anon, authenticated;
  REVOKE ALL ON ALL SEQUENCES IN SCHEMA public FROM anon, authenticated;
  REVOKE ALL ON ALL FUNCTIONS IN SCHEMA public FROM anon, authenticated;
  ALTER DEFAULT PRIVILEGES IN SCHEMA public REVOKE ALL ON TABLES   FROM anon, authenticated;
  ALTER DEFAULT PRIVILEGES IN SCHEMA public REVOKE ALL ON SEQUENCES FROM anon, authenticated;
  ALTER DEFAULT PRIVILEGES IN SCHEMA public REVOKE ALL ON FUNCTIONS FROM anon, authenticated;
  ```
  Verified via `has_table_privilege('anon', …, 'SELECT' | 'INSERT')` returning `false` for every check. **If we later want to use Supabase REST for any reason** (Supabase Auth client-side, Realtime, Storage public buckets, etc.), grants need to be re-applied per-table with appropriate RLS policies. Don't blindly `GRANT ALL`.

- **Master data parties (Customers / Suppliers) are for regulars only — walk-in / one-time parties NOT represented in master tables.** Forcing every sale/purchase party through a master record creates friction that doesn't match jewelry-workshop reality. The dual-path party model lands in Phase 3 (Sales) and Phase 4 (Purchases): each Sale/Purchase row gets either `customerId` (FK to a known regular) OR a free-text `partyName` + `partyPhone` pair (one-time / walk-in). The Customers page therefore intentionally has no Excel-bulk-import yet; regulars are entered manually as they emerge. Discovered Phase 2.1 planning. Cross-references: Phase 3 / Phase 4 implement the autocomplete + free-text fallback.

- **Schema extraction pattern for forms with Server Actions.** Zod schemas cannot be exported from files carrying the `'use server'` directive — Next.js compiles non-function exports into client-reference stubs, replacing the actual schema with a serialized handle. When the client form modal imports the "schema," it receives a stub that fails the `zodResolver`'s `isZod4Schema` check (`'_zod' in schema`) at module load with **"Invalid input: not a Zod schema."** Convention going forward: each entity gets a `schema.ts` (no directive — pure validation), `actions.ts` (`'use server'`, imports schema), and form modal (`'use client'`, imports schema). **Same rule applies to type-only exports:** Phase 2.3 originally put `export type { EmployeeForClient }` inside `actions.ts` — Next.js's actions loader can't handle that either, producing a runtime `ReferenceError: EmployeeForClient is not defined` when the form modal imported it. Fix: move shared client-facing types (along with any serializer helpers like `serializeEmployee`) into a separate non-server module (`types.ts`). Pattern committed for Customer in Phase 2.1; Suppliers (Phase 2.2) and Employees (Phase 2.3) follow the same layout — Employees additionally has `types.ts` for `EmployeeForClient` + `serializeEmployee`. Cross-reference Sales (Phase 3) and Purchases (Phase 4) when those land. Discovered Phase 2.1 Task 7; type-export trap discovered Phase 2.3.

- **Prisma enum value imports pull the entire Prisma client runtime into the client bundle.** Phase 2.3 originally had `import { EmployeeType } from "@/generated/prisma"` (value import) at the top of `schema.ts` plus a re-export for components. `EmployeeType` is a runtime const object in Prisma 7's client; the value import drags `@prisma/client/runtime/client.mjs` (which uses `node:module`) into every chunk that imports schema.ts. Through schema.ts → form-modal.tsx → page.tsx the Node-only module ended up in the client bundle, and the first GET /employees returned 500 with `Module not found: node:module`. Rule: **never `import { EnumName }` as a value from `@/generated/prisma`** in any module reachable from a client component. Use `import type { EmployeeType } from "@/generated/prisma"` for type-only references (stripped at compile time, zero runtime cost), and `z.enum(["FIXED", "LABOUR"])` with literal strings for runtime validation. Same rule applies to any Prisma runtime construct (`PrismaClient`, `Prisma.XyzScalarFieldEnum`, etc.) reachable across the boundary. Adjacent class of bug to the schema-extraction pattern above — both about "what gets pulled across the server/client boundary by an incidental import." Discovered Phase 2.3 build smoke.

- **BigInt currency fields stay BigInt only at the Prisma boundary — schema validates plain `number`, action converts at the edges.** `Employee.monthlySalary` is `BigInt? @map("monthly_salary")` in Prisma (paise integer math, future-safe for any salary range). The naive shape — a zod `.transform(v => BigInt(Math.round(v * 100)))` directly in the schema — broke client→server saves: the client form's `handleSubmit` produced a BigInt, then passed that BigInt to the server action, which re-ran `safeParse(input)` and rejected the BigInt with `Expected number, received bigint`. The fix is a three-layer pipeline: (a) **schema** validates rupees as `z.number().int().positive().nullish()` — input AND output shapes are both `number | null`, so client send + server re-parse stay aligned; (b) **action** has a local `toPrismaData()` / `buildSaleData()` helper that converts rupees → `BigInt(Math.round(value * 100))` paise at the Prisma boundary only; (c) **action return** runs a `serializeEntity()` helper from `types.ts` / `sale-helpers.ts` that converts the Prisma row's BigInt back to `number` for JSON safety, returning `EntityForClient` instead of the raw row. JSON cannot encode BigInt; React Server Components' Flight serializer cannot either. **General rule for any future BigInt column**: keep the wire format JSON-safe at both client/server crossings; do the BigInt conversion exactly at the Prisma write and undo it exactly at the action's return. Discovered Phase 2.3 build (Playwright smoke caught the wire-format mismatch). **Extended Phase 3.1** to the Sale entity — three BigInt columns per row (`rate`, `discount`, `total`) all flow through the same pipeline: schema keeps them as `z.number().nonnegative()`, `buildSaleData()` does the BigInt conversion *and* computes the stored total in BigInt math, `serializeSale()` returns `Number` to the client. Every subsequent monetary field in Phase 3.2+ / Phase 4 / Phase 6 follows the same shape.

- **Dual-path party model on `Sale` — denormalized snapshot + nullable FK.** The Sale row has three party-related columns: `customerId` (FK to `Customer`, nullable, `onDelete: SetNull`), `partyName` (required string), and `partyPhone` (optional string). Three operating modes: (1) **linked customer** — `customerId` is set and `partyName`/`partyPhone` are *server-side snapshots* of `Customer.name` / `Customer.phone` at sale time. The form may pass values for these fields, but the action's `buildSaleData()` overwrites them by re-reading the customer row (server is source of truth for FK-linked party data); (2) **walk-in** — `customerId` is null and the two strings are the only identity; (3) **defensive** — if a customer is ever hard-deleted (currently only soft-delete exists, but the FK behavior should survive future schema changes), `onDelete: SetNull` zeroes the FK while leaving `partyName`/`partyPhone` intact, so sale history is preserved. Snapshot pattern is what lets a customer rename (e.g. typo fix) without retroactively rewriting historical sale displays — the row carries the name as-of-the-sale. Discovered Phase 3.1 planning; matches the "regulars only" Phase 2.1 reframe — see the earlier "Master data parties" entry.

- **`Sale.total` is stored at write time, not derived on read.** Computed in the action's `buildSaleData()` helper as `BigInt(qty) × ratePaise − discountPaise` and persisted as a BigInt paise column. Alternative — compute on read — would be simpler at the action layer but loses audit history: if the discount/total logic ever changes (e.g. rounding rules, future per-line-item structure), historical sales would silently shift to the new formula. Stored totals also enable straightforward `SUM(total)` aggregations for dashboards / reports without expression complexity. Trade-off: total must be recomputed on every `updateSale`, and the discount-exceeds-line-total guard fires at the action layer (not the schema) because the formula needs the parsed numbers in scope. Acceptable for ~100 sales/month. Status remains derived (see CLAUDE.md §5) — different concern: status reflects payment/return state which DOES change over time, total reflects the original transaction terms which shouldn't. Discovered Phase 3.1 planning.

- **Per-payment audit trail via `SalePayment` child table — aggregation over column.** Phase 3.2 stores each payment as a separate row in `sale_payments` with `saleId` FK (`onDelete: Cascade`), `amount` BigInt paise, and the standard soft-delete column. `paidAmount` per sale is derived live via `SUM(SalePayment.amount WHERE deletedAt IS NULL)`, never cached on `Sale`. **Rejected alternative**: cached `paidAmount` column on `Sale` updated via trigger or action — adds drift risk (action forgets to update on a path; trigger gets bypassed by raw SQL fix-ups) for negligible aggregation cost at the scale we're at (~100 sales/month × handful of payments each = ~300 SUMs per page render, easily under 10ms with the `@@index([saleId])`). Per-row history is essential for the business — "when did the customer last pay you, and how much?" is the core question this entity exists to answer. Status derives via the same shape — `computeSaleStatus` reads `paidAmount` aggregation + (future) `returnTotal` aggregation, never a stored status field. Discovered Phase 3.2 planning.

- **Overpayment blocked at the action layer, NOT the schema.** Payments exceeding `total − paidAmount` are rejected by `createSalePayment` with `{ ok: false, errors: { amount: ["Exceeds remaining balance. Outstanding: ₹X"] } }` — the formatted outstanding helps the user identify what would have been accepted. **Why action and not schema**: the constraint needs the parsed `amount` + the sale's `total` + the current sum-of-payments in scope; the schema doesn't query the DB. **Why block at all** (vs. allowing overpayment with refund_due status): in practice, overpayment is either (a) a data entry error — catch it immediately and force the user to confirm intent; or (b) the customer paid extra and a refund is owed — which is a return-driven flow handled in Phase 3.3 via `SaleReturn`. Conflating "overpaid by mistake" with "customer overpaid intentionally" would lose the distinction. **`refund_due` status is reserved for return-driven refunds, never payment-driven overpayment.** Cross-reference: Phase 3.3 will add `SaleReturn` and `returnTotal` aggregation; `refund_due` activates there. Discovered Phase 3.2 planning.

- **Payments are immutable — soft-delete + create new, no update.** No `updateSalePayment` action exists. To correct a wrong payment: soft-delete the bad entry (sets `deletedAt`; row stays in the table but is filtered out of `paidAmount` aggregation via `WHERE deletedAt IS NULL`) and create a new correct payment. Both events stay in the history list visible in the detail modal's payment panel. **Why**: preserves audit trail (you can see "wrong ₹500 entry was reversed, replaced with ₹450") — important when reconciling with the customer's own records. **Trade-off**: slightly more UI work for corrections; the user clicks × on the bad row and creates a new one rather than editing in place. The Phase 3.2 inline panel makes this two-click flow ergonomic — × → Confirm reverse → form already expanded with "Pay full balance" ready. Discovered Phase 3.2 planning.

- **Live-updating modal pattern — derive viewed entity from id + parent data, not local snapshot.** When a detail modal shows data that can change via inner UI actions (Phase 3.2: payments; Phase 3.3: returns; Phase 3.4: receipts; future status/balance changes), holding the entity as `useState<EntityForClient>(viewing)` makes the modal show **stale** data after `router.refresh()` — the parent re-fetches, but the modal's local state still references the pre-mutation object. **Canonical fix**: `useState<string | null>(viewingId)` + derive via `parentDataArray.find(x => x.id === viewingId) ?? null`. After mutations call `router.refresh()`, the parent re-fetches → the derived value automatically resolves to the *updated* row → modal re-renders with fresh status / paidAmount / payments / chip in title — all without close-and-reopen. See `sales-table.tsx` Phase 3.2 implementation as the canonical example. **Use this pattern for Phase 3.3's return panel and Phase 3.4's receipt panel**, plus any future inline-edit-in-detail flows. The Phase 3.1 `viewingSale` state worked because nothing inside that modal mutated it; Phase 3.2's payment-panel broke that invariant, so the pattern needed to change. Discovered Phase 3.2 build (Playwright walkthrough's Step 5 critical assertion would have caught the bug if I hadn't fixed it preemptively).

- **RHF triple-generic pattern for transform-emitting schemas.** When a zod schema uses `.transform(...)` (as ours does to coerce `""` → `null` on optional fields), the input and output types differ — `phone: string | null | undefined` going in, `phone: string | null` coming out. RHF v7's `useForm<TFieldValues>` defaults `TTransformedValues = TFieldValues`, which mismatches the resolver's actual return shape. The fix is the **explicit triple generic**: `useForm<FormInput, unknown, FormOutput>` where `FormInput = z.input<typeof schema>` (form's pre-transform value type, allows `defaultValues: { phone: '' }`) and `FormOutput = z.output<typeof schema>` (post-transform shape passed to `onSubmit`). Without it, TypeScript rejects both `defaultValues` and `handleSubmit`. Committed for the Customer form modal in Phase 2.1 Task 4; same pattern for every form modal going forward.

- **Optional-field edit semantics — empty form field means NULL, not "preserve old value."** Customer/Supplier/Employee input schemas use a `.nullish().transform((v) => (v === undefined || v === null || v === "" ? null : v))` pattern (with `.pipe(z.union([z.null(), z.string().email(...)]))` for email so format validation runs after the empty-string normalization). This emits `null` for cleared inputs. Prisma's `update.data` treats `undefined` as "skip this field" but `null` as "set column to NULL" — so emitting `null` makes clearing-via-edit-form actually clear the DB column, not silently retain the stale value. The previous `.transform(() => undefined)` pattern from the Phase 2.1 spec example had this bug. Output type for each optional field is `string | null`, never `string | undefined`. Discovered Phase 2.1 Task 3.

- **Test infrastructure: Vitest + mocked Prisma, no real DB.** All Phase 2.2.5 tests run against `vitest-mock-extended`'s deep-mocked PrismaClient (`src/lib/__mocks__/prisma.ts`); real-DB integration tests are deferred to Phase 8 polish. Component tests use `@testing-library/react` + `user-event` with mocked `next/navigation` and mocked server actions. **Mock at the closest meaningful boundary**: `vi.mock("@/lib/auth-guards", ...)` rather than `vi.mock("@/lib/auth", ...)` — smaller surface, single export, no Auth.js factory coupling. The schema/action/component category split + the canonical mock patterns are documented in `docs/TESTING.md`.

- **Three pre-test extractions to canonical helpers (Phase 2.2.5 Tasks 2–4).** Before tests were written, three patterns were extracted from the duplicated Customer/Supplier code so tests would validate the canonical code, not the duplication: (a) `requireSession()` → `src/lib/auth-guards.ts` (was inlined in two actions.ts files); (b) `<Field>` → `<LabeledField>` in `src/components/labeled-field.tsx` (was duplicated as ~25-line subcomponent in two detail modals); (c) form-control styling → `<FormLabel>` / `<FormInput>` / `<FormTextarea>` / `<FormError>` in `src/components/form-controls.tsx` (was four exported `FIELD_*` className constants per form modal, ~20 lines each). Net change: -32 LOC project-wide; each entity's form-modal dropped from 240 to 191 LOC.

- **Test mock reset conventions split by test category.** Action tests reset Prisma at module scope (`beforeEach(mockReset(prisma))` in `src/lib/__mocks__/prisma.ts`) and reset `requireSession` / `revalidatePath` at file scope. Component tests reset all mocks inside the `describe` block via `beforeEach(vi.clearAllMocks)`. Different mock surfaces (deep-mock vs shallow `vi.fn()`s), different reset semantics; both correct for their case. Don't mix the two reset approaches within one file. See `docs/TESTING.md` for the table.

- **`SaleReturn` as a separate child table, not negative-direction `SalePayment` entries.** Returns are conceptually distinct from payments: one represents "product back," the other "money flow." Mixing them in `SalePayment` would have required type-discrimination on every query and lost the audit-category distinction — "show all returns this month" would become a filter-then-aggregate instead of a direct query. Separate `sale_returns` table preserves the categorical split and keeps two clean per-sale aggregations: `returnTotal = SUM(SaleReturn.refundAmount)` (product side, value-out) vs. `paidAmount = SUM(SalePayment.amount WHERE type=PAYMENT) − SUM(... type=REFUND)` (money side, net). Pattern matches Phase 3.2's `SalePayment`-as-child-table reasoning — aggregate-on-read beats cached columns for the workshop-scale workload. Discovered Phase 3.3 planning.

- **Refunds as `REFUND`-type `SalePayment` entries, NOT a separate `SaleRefund` table.** A refund IS a payment in the opposite direction. Same form (date + amount + note), same workflow (record event + soft-delete + recreate for corrections), same audit history. Adding a `type` enum (`PaymentType { PAYMENT, REFUND }`) on `SalePayment` keeps the payment ledger unified — users always see "Customer paid ₹2,400 on Mar 1, refunded ₹400 on Mar 5" as a single chronological stream. Net `paidAmount = SUM(PAYMENT.amount) − SUM(REFUND.amount)`, both filtered for non-deleted. **Rejected alternative**: a parallel `sale_refunds` table that fragmented the payment history into two streams users would always want to view interleaved. The Phase 3.3 migration adds the `type` column with `@default(PAYMENT)` so existing Phase 3.2 rows backfill correctly (verified via Supabase MCP: 3 pre-existing rows all backfilled to `PAYMENT`, 0 `NULL`, 0 `REFUND`). Single table + discriminator is cleaner. **Note**: this differs from `SaleReturn` (separate table) because Returns and Payments are categorically different concerns, while Payments and Refunds are the same concern in opposite directions. Discovered Phase 3.3 planning.

- **`refund_due` fires on `paidAmount > effectiveTotal` (where `effectiveTotal = total − returnTotal`).** Clean semantic: "the customer has paid more than the now-reduced amount owed." This is the only condition that triggers `refund_due` — pre-Phase-3.3 placeholder logic in `computeSaleStatus` triggered on `balance < 0` which was conceptually the same thing but worded around payments, not returns. The Phase 3.3 rewrite makes the return-driven causation explicit. **Edge case**: full return with no payment (`returnTotal === sale.total, paidAmount === 0n`) resolves to `completed` (sale is closed, nothing was ever owed back) — the `returnTotal > 0n` guard distinguishes "fully returned" from "degenerate ₹0 sale" (which stays `pending`). The full-return-no-payment case is unreachable in production because `createSaleReturn` validates cumulative `refundAmount ≤ sale.total`, but the function handles it gracefully for direct-API safety. Discovered Phase 3.3 planning; tested across 16 sale-status boundary cases.

- **Returns are immutable — soft-delete + create new (same as payments).** No `updateSaleReturn` action exists. Wrong return → soft-delete the bad entry (sets `deletedAt`; row stays but is filtered out of `returnTotal` aggregation) + create new correct return. Audit trail preserved. **Cross-row status interaction**: soft-deleting a return changes `effectiveTotal` which can flip status in either direction — `refund_due → completed/partial` when undoing a return whose refund hadn't been issued yet, `completed → partial` when undoing a return that had reduced effectiveTotal to match paidAmount, etc. Same applies to soft-deleting a `REFUND`-type `SalePayment` (Phase 3.3 walkthrough Step 12 exercises this: a sale that's `completed` after refund issuance flips back to `refund_due` if the refund payment is soft-deleted while the return remains active). The `viewingSaleId` live-update pattern handles the chip recomputation without modal close-and-reopen. Discovered Phase 3.3 planning.

- **Unified `computeTransactionStatus` helper for Sales + Purchases.** Phase 4 introduced Purchases as a structural mirror of Sales. The status math is identical across both entities (`pending` / `partial` / `completed` / `refund_due` derived from `total`, net `paidAmount`, `returnTotal`) — no entity-specific divergence in thresholds or edge cases. Decision: rename `src/lib/sale-status.ts` → `src/lib/transaction-status.ts`, rename `computeSaleStatus` → `computeTransactionStatus`, keep `SaleStatus` and `PurchaseStatus` as **type aliases** in each entity's helpers (`sales/sale-helpers.ts` re-exports `type SaleStatus = TransactionStatus`; `purchases/purchase-helpers.ts` declares `export type PurchaseStatus = TransactionStatus`) for call-site readability. **Rejected alternative**: a thin `purchase-status.ts` that just re-exports the Sales helper — adds ceremony with no benefit since the math is genuinely shared. **Rejected alternative B**: two parallel implementations — invites silent drift if one is patched without the other. **Trade-off**: tight coupling between Sales and Purchases status logic. **Rationale**: divergence in status rules between sales and purchases is implausible at this scale; if it ever happens, the single file is the obvious extension point with predictable refactor cost. The unified `<TransactionStatusChip>` (in `src/components/`, not in either entity folder) follows the same pattern — single component, both folders import it. Discovered Phase 4 planning.

- **UI label inversions for supplier-direction (Purchases) — same enum, same status, opposite money direction.** Phase 4 introduced semantic inversions where the same `PaymentType` enum + same `refund_due` status mean opposite money-direction depending on which entity owns the row. The mental model that ties it together: **red = money OUT of shop; blue/secondary = money IN to shop, regardless of which entity owns the row.** Concrete inversions wired into `purchases/payment-panel.tsx`:
  - "Outstanding" (Sales: customer owes shop) → "Owed to supplier" (Purchases: shop owes supplier)
  - "Refund owed" (Sales: shop owes customer back) → "Refund expected" (Purchases: supplier owes shop back)
  - Indicator color when refund_due fires: `text-error` (red, Sales) → `text-secondary` (blue, Purchases) — money IN is positive
  - Trigger button label when refund_due fires: "+ Issue refund" (Sales) → "+ Record refund received" (Purchases)
  - REFUND-row label in history: "Refund" (Sales) → "Refund received" (Purchases)
  - REFUND-row text color: `text-error` (Sales) → `text-secondary` (Purchases)
  - REFUND-row amount prefix: `−` (Sales: money out) → `+` (Purchases: money in)
  - Server error message in `payment-actions.ts` overpayment rejection: "Outstanding: ₹X" (Sales) → "Owed to supplier: ₹X" (Purchases)

  The label inversions are the **most visible Sales-vs-Purchases difference** from the user's perspective; the schema clones cleanly, the actions clone cleanly, but copy-paste regressions in the UI would silently leak Sales-direction language into the Purchases panels. The Phase 4 walkthrough's Step 7 + Step 11 explicitly assert on the inverted labels (and the `payment-panel.test.tsx` does dual-assertion: positive on the new label + negative on the old). Discovered Phase 4 planning.

- **`<PartyPicker>` now exists twice (Sales + Purchases) — deliberately NOT extracted.** Both `sales/party-picker.tsx` and `purchases/party-picker.tsx` are ~177-line structural clones with parameter substitution (`customerId ↔ supplierId`, `customers ↔ suppliers` prop, `CustomerOption ↔ SupplierOption`, `"Customer name or walk-in" ↔ "Supplier name or walk-in"`, `"Clear linked customer" ↔ "Clear linked supplier"`). **Rejected alternative**: extract now to `<PartyPicker directory={Array<{id, name, phone | null}>} valueShape={...} />` after two instances. **Why hold off**: with only two data points (master-data customers, master-data suppliers), the right abstraction shape isn't clear — what fields belong on `directory` items? does the picker need to know the entity name for placeholder/aria text, or does the parent inject those? **Extraction trigger**: Phase 6 (karigar payments) is likely to add the third party-picker instance for selecting an employee/karigar from the `employees` table. At that point three concrete instances will make the right generic shape obvious. Premature abstraction across two instances risks locking in a shape that breaks for the third — and the cost of carrying the duplication for one more phase is bounded (177 lines × 2 files). Discovered Phase 4 build.

- **Four-role enum replacing `ADMIN | STAFF`.** Phase 5 replaced the binary role split with four explicit roles matching the company's operational shape: `ADMIN` (full access), `PURCHASE_DEPT` (Purchases + Suppliers full CRUD), `LABOUR_MGMT` (Employees full CRUD including salaries — strictly no Sales/Purchases visibility), `CASTING_PLATING_MGMT` (Casting + Plating routes; pages deferred to Phase 9). `STAFF` removed entirely — was never used in production (verified zero rows before drop). `User.role` is required-on-insert (no DB default); every new user must explicitly choose a role. Migration applied via two SQL files: `20260515182838_role_add_new_values` (additive `ADD VALUE` × 3) + `20260515183353_role_remove_staff` (CREATE new type → ALTER COLUMN → DROP old type, hand-rolled because non-interactive shells can't satisfy `prisma migrate dev`'s breaking-enum confirmation prompt). Discovered Phase 5 planning.

- **`requireRole(allowedRoles[])` pattern at every server action.** Replaces per-action `requireSession()` for role-gated actions. The signature is `requireRole(allowedRoles: Role[]) => Promise<Session>`; throws `Unauthorized` (no session) or `Forbidden` (wrong role); both halt before any DB work. Pattern at every action: `await requireRole(["ADMIN", "PURCHASE_DEPT"])` as the first await. **Why explicit at the call site**: reading the action's first line tells you exactly which roles can invoke it — the access matrix is local to the action, not buried in a shared config. **Why kept `requireSession()`**: still useful for endpoints where any authenticated user is sufficient (currently none — Phase 5 migrated everything, but the helper stays for one-off cases). Definition lives in `src/lib/auth-guards.ts`. Discovered Phase 5 planning.

- **Defense in depth for route access — sidebar filters AND proxy redirects.** UX layer: `src/app/(app)/sidebar.tsx` filters `NAV_ITEMS` by `user.role.allowedRoles.includes(...)` so users never see options they can't use. Security layer: `src/proxy.ts` carries a `ROUTE_ROLES` map; unauthorized URL access (manual typing, bookmarks, deep links) bounces to `/dashboard` before any page module loads. **Both required**: sidebar alone leaves manual-URL access unblocked; proxy alone leaves stale nav items in the UI. The matrix is duplicated in three places — proxy's `ROUTE_ROLES`, sidebar's `NAV_ITEMS[].allowedRoles`, and per-action `requireRole([...])` calls — accepting some redundancy to keep each layer's rules local and readable. If the matrix grows complex enough that drift becomes a real risk, extract to a single source-of-truth `src/lib/rbac-matrix.ts` and have all three layers consume it. Not worth doing yet at four roles × ~8 routes. Discovered Phase 5 planning.

- **Single `/dashboard` route with role-aware server-side rendering.** Not multiple routes (`/dashboard/admin`, `/dashboard/purchase`, etc.). The page reads `session.user.role` and `switch`es to one of `AdminDashboard | PurchaseDashboard | LabourDashboard | CastingPlatingDashboard`, each a server component that fetches only the data its cards need. **Why single route**: simpler URL structure (every role lands on the same path after login, so `proxy.ts` can put `/dashboard` in `ROUTE_ROLES` once with all four roles); centralised role-switching logic; bookmarks survive role changes (admin demoted to PURCHASE_DEPT keeps `/dashboard` bookmark working). Trade-off: a single switch statement that has to stay exhaustive — TypeScript catches non-exhaustive switches via the `never` assignment in the default branch. Discovered Phase 5 planning.

- **Phase 5 ships minimal landing pages; full Recharts dashboards deferred.** Each role gets a welcome header + 2–4 summary cards (counts, current-month totals, "owed to suppliers" for the purchase role). No Recharts, no monthly trend lines, no payable timelines, no aging buckets. Real charting (monthly sales trend, supplier payable aging, salary expense trend) is deferred until Phase 11 (full Recharts dashboards from original plan). **Why defer**: the right charts depend on real usage data informing which views matter — three months of production transactions tells us more than upfront design. Cards exist now to give every role somewhere to land after sign-in; charts join when the user has real data to plot. Discovered Phase 5 planning.

- **Walk-in auto-promotion via phone identity (Phase 6).** When a sale or purchase is saved as walk-in (no explicit `customerId`/`supplierId`) but WITH a populated `partyPhone`, the server transactionally either links to an existing Customer/Supplier (matched by normalized phone) or auto-creates one. Walk-ins without phone stay as snapshot-only entries — no auto-creation since identity cannot be confirmed. The whole flow (`tx.customer.findFirst` → optional `tx.customer.create` → `tx.sale.create`) runs in `prisma.$transaction`, so customer/supplier creation and sale/purchase creation succeed or fail together. **Rationale**: user-stated workflow problem — *"if I enter xyz in sales as walkin it should be updated into the customers same for purchase so that the name does not change if they are same"*. Phone is the most reliable identity proxy at this workflow's scale (~100 transactions/month, small known party list, no GST/PAN identifiers tracked yet). The action *also* re-snapshots `partyName` from the canonical Customer/Supplier row on every save — so the second walk-in with the same phone but a different typed name carries the canonical name, not the new typed string. Discovered Phase 6 planning; verified end-to-end via the production walkthrough's step 2.

- **Phone normalization at every boundary (Phase 6).** `normalizePhone(s)` in `src/lib/phone.ts` strips whitespace, dashes, and parens; preserves leading `+` for international format; returns `null` for empty/whitespace-only input. Idempotent — running normalization on already-clean input is a no-op. Applied at **every storage boundary** (`Customer.phone`, `Supplier.phone`, `Sale.partyPhone`, `Purchase.partyPhone` schema transforms) AND at **every lookup boundary** (auto-promotion `findFirst` queries, party-picker phone-prefix matching). Without symmetric normalization, lookups miss already-stored records (typed `9876-543-210` would not match stored `9876543210`). The `+`-preservation is deliberate: international numbers with a leading `+` are kept distinct from the same digits without `+`, so `+919876543210` and `919876543210` would NOT match each other — accepted trade-off, no current Indian-only workflow uses the leading `+`. Discovered Phase 6 planning.

- **Party picker phone-prefix matching (Phase 6).** Typing digits into the party picker now surfaces customers/suppliers whose **normalized phone starts with** the typed prefix, in addition to the existing name-substring match. **Pure-letter queries skip phone matching entirely** (the picker only considers phone candidates when the query contains at least one digit) — avoids spurious hits if a stored phone ever happened to contain letters. Mixed-alphanumeric queries match by name only (the query is treated as a name substring; the picker doesn't try to extract digits from a mixed string). Both the candidate phone and the query digits are run through `normalizePhone` before comparing, so `9876-543-210` stored matches `9876` typed and vice versa. The "Use as walk-in: …" footer entry stays at the bottom regardless of match results. Discovered Phase 6 planning.

- **Multi-item line items via child tables (Phase 7).** `SaleLineItem` and `PurchaseLineItem` tables, one row per line item, FK to parent with `onDelete: Cascade`. **Rejected alternatives**: (a) JSON column on parent — makes aggregation queries painful ("total revenue by item type" becomes JSON traversal instead of JOIN); query-tooling support degrades; types lose precision since JSON columns can't enforce `BigInt`/integer constraints. (b) Single denormalized item per parent — the Phase 3.1 lock that real usage proved wrong within four phases. The chosen child-table pattern matches every other relational shape in this codebase (payments, returns) and lets the same `prisma.$transaction(tx => ...)` wrapper handle the create/update flow with no new infrastructure. Discovered Phase 7 planning.

- **Sale-level discount only, no per-line discount (Phase 7).** `Sale.discount` and `Purchase.discount` apply to the whole-sale subtotal; `SaleLineItem`/`PurchaseLineItem` carry only `itemDescription`, `qty`, `rate` — no discount column per line. **Rationale**: matches the workshop's actual invoicing practice ("₹13,500 of items, give it for ₹13,000" — the discount is whole-invoice, not per-line). Per-line discount would force the user to decide which line absorbs the discount; that decision has no business meaning when the discount was negotiated against the whole sale. **If per-line negotiated discounts surface as a real workflow later**, the schema extends naturally with a `SaleLineItem.discount` column (and the action's subtotal math becomes a per-line `(qty × rate - discount)` sum). For now: `Sale.total = SUM(lineItems.qty × lineItems.rate) - Sale.discount`. Discovered Phase 7 planning.

- **Line items hard-deleted on parent edit — replace-all pattern (Phase 7).** Update sequence: `tx.saleLineItem.deleteMany({ where: { saleId } })` then `tx.sale.update({ data: { lineItems: { create: [...] } } })` inside `prisma.$transaction`. **Rejected alternative**: soft-delete each line item with `deletedAt` + diff-then-update — adds `deletedAt IS NULL` filters everywhere with no audit value (line items are subordinate to the parent Sale/Purchase; the audit-relevant unit is the parent transaction, not its line breakdown). **Sale soft-delete still preserves line items** in DB because `onDelete: Cascade` fires only on actual delete; soft-delete sets `deletedAt`, the line items remain attached and are filtered out of the app by the parent query's `deletedAt: null` clause. Hard-delete (which the app never performs) would cascade and remove the lines. Pattern atomically replaces the entire line set on every edit — no diffing, no per-line update logic, no edge cases around "what if the user removed a line that has dependent rows." Discovered Phase 7 planning.

- **Phase 3.1's single-item decision proved wrong by real usage — institutional-memory note (Phase 7).** The Phase 3.1 lock said "single qty/rate/itemDescription per Sale because the current workflow does one-item invoices." Real production usage produced multi-item invoices within four phases (the "fzd / sdf + 2 more" stray row was the first sign), forcing the Phase 7 restructure with two migrations and a major UI rebuild. **Cost**: ~6 hours saved in Phase 3.1, ~8 hours spent in Phase 7 to undo + rebuild + migrate data + rewrite 144 tests. **Net**: slightly negative on the bookkeeping; the alternative (over-engineering line items in Phase 3.1 before knowing the workflow needed them) might have produced an abstraction that didn't match the eventual shape either. **Tentative rule**: schema-shape decisions justified by "current workflow doesn't need this" should be revisited after real production usage, not before; locking them too early commits to a shape before the workflow is known. **Counter-rule**: don't add flexibility unprompted — three similar lines is better than a premature abstraction. The honest answer is the locks are right in expectation, sometimes wrong in retrospect; document the lineage so the same trade-off isn't re-evaluated from scratch every time.

- **Bill discriminator pattern over polymorphic FK (Phase 8).** One Bill table that can attach to multiple parent kinds (Purchase, PurchasePayment, CastingEntry, PlatingEntry, ...) had two shapes available: (a) a separate `*_bills` join table per parent kind, each with a real FK + CASCADE; (b) one Bill table with a string discriminator (`attachedToType`) + an unconstrained `attachedToId` carrying the parent's cuid. **Chose (b)**: adding a new attached-to kind in a future phase = extend the `ATTACHED_TO_TYPES` enum in `src/app/(app)/bills/schema.ts` + add a row to the role matrix in `src/lib/bill-access.ts`. The Bill table itself never changes shape, and the access matrix is the single source of truth for who-can-see-what. **Trade-off accepted**: no DB-level cascade on parent delete — orphan reference detection is the caller's responsibility. At ~100 transactions/month with rare hard deletes (everything is soft-delete), the orphan window is tiny; a deferred cleanup job (see Deferred items) can reconcile if needed. The polymorphic-FK alternative would have required 4+ join tables today and another every time a new kind is added. Discovered Phase 8 planning.

- **R2 key includes a UUID independent of the Bill row id (Phase 8).** The natural shape would be `bills/YYYY/MM/<billId>-<filename>`, but Prisma's `@default(cuid())` generates the row id at INSERT time — and `r2Key` is `@unique` and required-non-null on the same insert. Pre-allocating a cuid client-side would either need a dependency on `@paralleldrive/cuid2` (not currently installed) or a homegrown cuid-shaped generator (rejected — reinvents the wheel). **Chose** `crypto.randomUUID()` for the key suffix: stdlib-only, collision-free at our scale, and decouples the R2 key from the Bill id so the action stays a single Prisma roundtrip. The Bill row's `id` and the key's UUID serve different purposes (DB PK vs. object name uniqueness); `@@unique` on `r2Key` is the DB safety net against collision. Discovered Phase 8 build.

- **R2 client construction is lazy (Proxy) — same rationale as the Prisma wrapper (Phase 8).** `src/lib/r2.ts` mirrors the lazy-init Proxy from `src/lib/prisma.ts`: env (`R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, etc.) is read on first property access, not at module load. Eager construction would crash Next.js 16's build-time page-data collection — the route module that imports `r2.ts` (via the Bill server actions) loads during static analysis, before runtime env-var injection. The pattern is now general: any SDK client wrapper that reads env vars at construction time should defer via Proxy. Documented in the §6 conventions as a general rule. Discovered Phase 8 build (verified by `next build` smoke).

- **Cloudflare R2 CORS configured via the S3 API, required an Admin-scope R2 token (Phase 8).** Browser → R2 PUT via presigned URL fails the CORS preflight without an explicit bucket CORS policy (the standard "Network error" symptom — `xhr.onerror` fires but the browser surfaces no actionable detail). Cloudflare R2 supports CORS configuration via the S3-compatible `PutBucketCors` command, BUT the credentials must come from an **Admin Read & Write** R2 token; the **Object Read & Write** token the app uses for presign + delete gets `AccessDenied`. **Resolution**: upgrade the token to Admin R&W temporarily, apply CORS, then revert the production env (Vercel + `.env.production.local`) to Object R&W (CORS rules persist independent of the token that set them). One-time setup; ongoing app traffic only needs Object R&W. Allowed origins applied: prod alias, git-main alias, `https://*.vercel.app` (preview deploys), `http://localhost:3000`, `http://localhost:3001`. Allowed methods: GET / PUT / HEAD / DELETE. Max age 1h. Discovered Phase 8 walkthrough.

- **Casting and plating as two separate entity types, not a unified table with a discriminator (Phase 9).** `CastingEntry` and `PlatingEntry` are structurally identical mirrors but live in their own tables (plus their own `*LineItem` + `*Payment` child tables). The unified-table alternative (one `OutsourceEntry` with `type: 'CASTING' | 'PLATING'`) was rejected: separate sidebar entries match separate workflow concepts in the workshop's mental model; per-entity URLs (`/casting`, `/plating`) are cleaner; per-entity action surfaces don't need a runtime branch on type; and future outsource categories (polishing, welding) extend by adding new entity types rather than overloading a discriminator column. **Trade-off accepted**: ~2,300 LOC of mirrored code per side. **Mitigation**: a reproducible mirror script (`scripts/_mirror-casting-to-plating.mjs`, gitignored under `scripts/_*.mjs`) generates plating files from casting with a protected-identifier substitution. Run after any casting-side change to keep plating in lockstep; verify post-mirror with `tsc --noEmit` + a grep for stray `casting`/`Casting` tokens outside the protected list. Discovered Phase 9 planning.

- **Single shared `CastingPlatingVendor` table covers both workflows (Phase 9).** Real outsourcing vendors often do both casting AND plating, so separate `CastingVendor` + `PlatingVendor` tables would have produced duplicate records for every overlapping vendor. The entry's table (`CastingEntry` vs `PlatingEntry`) determines the workflow; the vendor record is the same person/shop regardless. `CastingPlatingVendor` has both `castingEntries: CastingEntry[]` and `platingEntries: PlatingEntry[]` reverse relations; the vendors list page shows precomputed counts of each per row. Trade-off: the table name is awkward ("CastingPlatingVendor"); reading it as "vendor shared by casting and plating" is the intent. If a third workflow lands (polishing, etc.) we either rename or accept the cumulative name. Discovered Phase 9 planning.

- **Weight stored as `Decimal(10, 3)` kg, not Int grams (Phase 9).** Money in this codebase is stored as `BigInt` paise (integer-storage pattern) — applying that consistency to weight would have meant `BigInt` grams (e.g., 2500g for 2.5kg). **Rejected**: the workflow speaks kg (the user reads "2.5 kg" not "2500 g"), invoices speak kg, and forcing a g↔kg conversion at every read+write boundary would add complexity without ergonomic upside. `Decimal(10, 3)` enforces the 3-decimal-place gram precision at the column level; `Decimal.js` handles arithmetic exactly. The pattern is symmetric with money (storage primitive that needs precision preservation) but uses a different primitive (`Decimal` for fractional units, `BigInt` for integer units). Discovered Phase 9 planning.

- **Rate stored as `BigInt` paise per kg — money pattern preserved (Phase 9).** Weight is `Decimal`; rate is `BigInt`. The Decimal × BigInt multiplication lives in exactly one place: `computeLineTotal(weightKg, ratePerKg): bigint` in `src/lib/weight-helpers.ts`. The helper does `weightKg.mul(ratePerKg.toString()).toDecimalPlaces(0, ROUND_HALF_EVEN)`. **ROUND_HALF_EVEN** (banker's rounding) was chosen over ROUND_HALF_UP — banker's rounding has no systematic bias when summing many rounded values (each `0.5` case rounds to the nearest even, balancing out across the dataset), while half-up biases positive over many transactions. At ~50 casting/plating entries per month with multiple line items each, the bias accumulates measurably over a year. **Verified via the 4 distinguishing cases** in `weight-helpers.test.ts` (`0.5→0`, `1.5→2`, `2.5→2`, `3.5→4`). Discovered Phase 9 planning.

- **Decimal columns serialise as strings (not numbers) at the action boundary (Phase 9).** `CastingLineItem.weightKg` reads as a Decimal in Prisma, serialises to a `string` (e.g., `"2.500"`) at the action's `serialize*Entry()` return, and the client formats via `formatKg(string)` for display. The naïve alternative — `Number(weightKg.toString())` at the boundary — collapses `"2.500"` to `2.5` and loses the gram-precision contract that the DB column enforces. JS Number can represent `2.500` numerically (it's an exact float) but cannot distinguish it from `2.5` once stringified; the string preserves the original digit shape. Symmetric pattern with BigInt → Number paise serialisation (both are "storage primitives that need precision preservation across the JSON wire"). Discovered Phase 9 build (would have shipped `2.5 kg` instead of `2.500 kg` in the table view otherwise).

- **No returns workflow for casting / plating (Phase 9).** Outsourced services don't have a returnable-goods analogue to `SaleReturn` / `PurchaseReturn`. The vendor performs a service; if the result is unsatisfactory, the typical remedy is rework (no money changes hands) or a refund (handled as a `REFUND`-type `CastingPayment` / `PlatingPayment`, mirroring the Sale / Purchase refund pattern via the shared `PaymentType` enum). **Rejected** the `CastingReturn` table because there's no defective-goods workflow to track quantitatively — the underlying entity is a job, not units. **Trade-off**: the `computeTransactionStatus` call site for casting/plating doesn't pass a `returnTotal` argument (defaults to `0n`), so `refund_due` only activates if a REFUND-type payment over-applies relative to the entry total. In practice this is rare; it's there for symmetry with the existing status machine. Discovered Phase 9 planning.

- **Forms moved to dedicated pages — Sales-only scope this phase (Phase 10).** The modal pattern outgrew multi-line forms when Phase 7 added line-item arrays — modal width + native scrolling + browser-history were all cramped. Form-as-page gives full-width layout, native scrolling, browser back/forward, and bookmarkable URLs. **Scoped to Sales** for Phase 10 per the explicit scope decision at build start; Purchases / Casting / Plating mirror lands in Phase 10.5 (mechanical, the shared components are proven). **Detail modal stays as a modal** even after the form moved — it's a small read-only surface where the modal pattern still works; converting it to a page would have meant a separate `/sales/[id]` route just to view, which doesn't add value. Discovered Phase 10 planning.

- **Detail modal → read-only (Sales, Phase 10).** Separates view from write. The modal previously carried four mutation responsibilities (Add Payment, Add Return, Replace Bill, Delete) which made it the de-facto workflow center; the user had to know to "open detail" to do anything. The new pattern: detail modal answers "what's in this sale?" (read-only line items, payments history, returns history, status chip); the row's Actions column answers "what do I want to change?" (`$ Pay` / `📎 Bill` / `↩ Return` icon buttons + the Edit link → `/sales/[id]/edit`). Cleaner mental model. **Purchases / Casting / Plating detail modals retain their mutation surfaces (PaymentPanel, ReturnPanel, embedded bill section) until Phase 10.5.** Discovered Phase 10 walkthrough Step 4 — explicitly verified zero mutation buttons inside the new sales detail modal.

- **Inline action buttons on transaction rows — three slots, lifted state (Sales, Phase 10).** Three icon buttons per Sales row in a dedicated Actions column: `[$ DollarSign] [📎 Paperclip] [↩ Undo2]`. **Discoverability beats density** for an internal tool with workshop-staff users — explicit affordances on every row beats a hidden context menu. **Modal state is lifted to the table component**: `paymentRowId` / `billRowId` / `returnRowId` are three string-or-null slots at the table level (one slot per modal type, with `activeRowId` determining which row's data feeds the modal). The active modal's data is derived live via `sales.find(s => s.id === paymentRowId)` — same Phase 3.2 derive-from-id pattern that prevented stale-snapshot bugs in the detail modal. **Avoided**: per-row state (would have been three modals × N rows on every render). Discovered Phase 10 planning.

- **Bill upload preview via browser-native APIs (Phase 10).** `URL.createObjectURL(file)` produces a blob URL that an `<img>` (for image MIMEs) or `<embed type="application/pdf">` (for PDFs) renders without any server round-trip. The user confirms the right file is selected BEFORE the modal triggers `prepareUpload` → R2 PUT → `confirmUpload`. Cleanup via `URL.revokeObjectURL` on unmount or file replacement prevents the blob-URL pool from leaking across many uploads. **Fallback** for non-image / non-PDF types (which shouldn't reach this point — the schema rejects them — but render a "no preview available" notice for robustness). `BillPreview` lives in `src/components/` and is shared between `BillActionModal` (table action button path) and the form page (when Phase 10.5 adds an inline bill section). Discovered Phase 10 walkthrough Step 8 critical check.

- **Action modal dispatch via prop injection (Phase 10.5).** `PaymentActionModal` / `ReturnActionModal` accept an `onSave: (data) => Promise<SaveResult>` prop instead of switching on `entityType` internally; `BillActionModal` accepts optional `onAttach: (entityId, billId) => Promise<{ok}>` / `onDetach: (entityId) => Promise<{ok}>` props for Casting/Plating's `billId @unique` FK management (Sales/Purchases use the discriminator-only attachment and pass neither). The internal chain (`getBillForEntity` → optional `onDetach` → `softDeleteBill` → `prepareUpload` → R2 PUT → `confirmUpload` → optional `onAttach`) stays entity-agnostic. **Why prop injection over a `switch (entityType)` inside the modal**: each branch would import the entity-specific server actions, transitively dragging all four entities' action modules into every modal's bundle even when the rendered instance only uses one. Prop injection keeps the modal pure and pushes the action choice to the caller (where the imports are already local). **Phase 10.6 just wires casting/plating tables to dispatch the right actions** — no modal rebuilds. Cross-entity contract tests live in `payment-action-modal.test.tsx` / `bill-action-modal.test.tsx` parameterised via `describe.each(ENTITY_MATRIX)`; chain ordering verified via `callOrder: string[]` accumulator. Discovered Phase 10.5 planning.

- **Sales bill-in-form retrofit (pre-upload during save) (Phase 10.5).** The Sales form page (`/sales/new`, `/sales/[id]/edit`) includes an inline bill section between line items and totals: file picker → `BillPreview`. On submit, the chain runs **after** `createSale | updateSale` resolves with the entry's id (the bill needs `attachedToType="SALE"` + `attachedToId=savedSaleId` to be set at `prepareUpload` time). Order: `createSale → prepareUpload → R2 PUT → confirmUpload → navigate`. **Error semantics**: if the upload chain fails AFTER the entry is created, the entry **stays saved** (no rollback) and a banner directs the user to the row-level 📎 action modal for retry — rolling back the entry would punish a valid save for a recoverable network/storage failure. Edit mode adds NEW bills only (re-use the row-level modal for replace/remove, which already handles the soft-delete + re-upload transient state via the discriminator). **Purchases bill-in-form deferred to Phase 10.6**; the row-level 📎 modal covers the gap. Discovered Phase 10.5 walkthrough Steps 11-12.

- **Mirror-script string-literal leaks not caught by `tsc` (Phase 10.5).** When mirroring entity types via search-and-replace (the canonical Phase 9 mirror pattern), **string literals inside JSX props** (`entityType="sale"`) and **data object string values** (`attachedToType: "SALE"`) are valid in both source and target discriminated-union types — TypeScript accepts both because each is a legal member of the union. `tsc --noEmit` cannot catch the leak. Phase 10.5 found `entityType="sale"` literals leaked into `purchases-table.tsx` after the mirror, attached 4 orphan `SALE`-typed Bill rows to actual purchase IDs in production before the walkthrough caught it. **Defences** (apply after every mirror):
  - **(a)** Grep the target for source-entity literals: `grep -rni 'entityType="sale"' src/app/\(app\)/purchases/` should produce zero matches.
  - **(b)** Walkthrough must exercise a cross-system data path that verifies the discriminator value in the DB (e.g., `SELECT "attachedToType" FROM bills WHERE "attachedToId" = '<new entity id>'`) — not just the UI behaviour, which renders correctly on both branches.
  - **(c)** Cleanup query for any data corruption is part of the walkthrough script (Phase 10.5: scrubbed 4 SALE-typed bills attached to purchase IDs via `UPDATE bills SET "deletedAt" = NOW() WHERE "attachedToType" = 'SALE' AND "attachedToId" IN (<purchase ids>)`).
  - **(d)** Add an explicit regression-guard test in the mirrored entity's table test: assert every action-modal mount receives the correct `entityType` prop. Example in `purchases-table.test.tsx`. Discovered Phase 10.5 walkthrough.

- **Casting + Plating UX mirrored to match Sales/Purchases pattern (Phase 10.6).** All four transactional entities now use the same UX: form-as-page + read-only detail modal + inline action buttons + inline bill section in form pages. Mirror approach: a new `scripts/_mirror-sales-to-casting.mjs` script transformed the Sales canonical files (Phase 10 + 10.5) into a starting point for Casting, then the existing Phase 9 `scripts/_mirror-casting-to-plating.mjs` produced Plating from Casting. Both gitignored under `scripts/_*.mjs`. **Manual review applied for entity-specific divergences**: weight inputs replace qty inputs (`Decimal(10, 3)` `step="0.001"` vs `Int step="1"`); vendor picker replaces customer picker (`CastingPlatingVendor` vs `Customer`); FK-based bill attachment replaces discriminator-only (the chain ends with `attachBill*To*Entry` after `confirmUpload`); Sale-direction "Outstanding" labels invert to vendor-direction "Owed to vendor" via the existing `PaymentActionModal` `owedLabel()` helper. **Why the mirror script is still valuable despite the substantial manual review**: the substitution handles ~70% of identifiers (component names, type names, action names, URL paths, helper imports) mechanically; the manual review focuses on the legitimate data-model differences. Without the script the alternative is a full hand-write of ~1,200 LOC. Discovered Phase 10.6 planning.

- **BillActionModal prop contract via short-circuit (Phase 10.6).** `onAttach` / `onDetach` props are optional. Calling pattern: Sales/Purchases pass neither (discriminator-only attachment is established by `prepareUpload`'s `attachedToType` + `attachedToId` args; no FK to manage); Casting/Plating pass both (the entry row's `billId @unique` FK has to be cleared via `onDetach` BEFORE soft-deleting the old bill, and set via `onAttach` AFTER `confirmUpload`). The modal's internal chain executes `onDetach` and `onAttach` only when they're defined. Single component handles both attachment patterns cleanly via `if (onAttach) { ... }` / `if (onDetach) { ... }` short-circuit. **Why this beats a `switch (entityType)` inside the modal**: keeps the modal's import graph pure — it never reaches into entity-specific action files. The caller (the table component) is the natural owner of "which entity-specific FK actions to wire," and the prop boundary makes the dependency explicit. Pattern established Phase 10.5 (with two entity types — Sales/Purchases — and absent props); extended Phase 10.6 (with two more entity types — Casting/Plating — and present props). Cross-entity contract tests live in `bill-action-modal.test.tsx` parameterised via `describe.each` + chain-ordering `callOrder: string[]` accumulator.

- **Documentation-vs-implementation drift surfaced in mirror script (Phase 10.6).** Phase 9's onboarding note specified that `castingPlatingVendor` (camelCase Prisma property) should be in the casting↔plating mirror's `PRESERVE_PAIRS` list — but the actual `scripts/_mirror-casting-to-plating.mjs` was missing the entry. Phase 10.6's first run of the mirror produced `platingPlatingVendor` in the target side, caught by 25 `tsc --noEmit` errors in `plating/actions.test.ts` + `plating/actions.ts`. Added the missing protect, re-ran the mirror. **General pattern**: when adding an onboarding note about a defence, immediately verify the defence exists in code, not just in docs. Documentation rot manifests at the moment of trust — when a future contributor (or the same person months later) believes the defence is in place because the doc says so, and skips the verification step. **Mitigation applied this phase**: the Tests phase added explicit regression guards in `casting-table.test.tsx` + `plating-table.test.tsx` that would catch the `entityType` leak class at unit-test time (the next-tier defence after the existing tsc + grep checks). Discovered Phase 10.6 mirror execution.

- **Form line items use the `md:contents` trick to switch between stacked-mobile and flat-desktop layouts in one JSX tree (Phase 11.2).** Each line item is an outer `grid grid-cols-1 md:grid-cols-[1fr_80px_120px_120px_40px]` with three direct children: (a) Description div, (b) a wrapping inner div `grid grid-cols-[1fr_1fr_44px] md:contents`, and (c) a `md:hidden` mobile-only line-total row. The inner div contains four children — qty, rate, desktop-only line-total (`hidden md:flex`), and the × remove button. On mobile: the outer is single-column; the inner is a 3-col sub-grid placing qty/rate/× on row 2; the mobile-only line total appears as row 3. On md+: the inner div's `display: contents` makes it transparent to the grid layout, so its four visible children land in outer columns 2/3/4/5 — preserving the desktop horizontal layout. The mobile-only line-total row is hidden via `md:hidden`. **Why not separate desktop + mobile JSX trees**: would have required maintaining two copies of every line item input — error-prone with RHF `useFieldArray`'s field key bookkeeping, since each input has to keep its `register(...)` call. The single-tree pattern uses one input per slot and lets CSS decide layout. **Why not just `grid-cols-1` on mobile**: the qty/rate/× row needs a 3-column sub-grid on its own row, otherwise qty wraps full-width below description (consuming 3× the vertical space). The inner grid provides exactly that — and `md:contents` removes the wrapper from layout when desktop wants flat columns. Casting/Plating mirror with outer `[1fr_110px_130px_130px_40px]` (slightly wider for kg/rate displays). The pattern is documented as a citable convention in `CLAUDE.md §6` so future entity additions follow it.

- **Master data mobile cards have NO inline action buttons (Phase 11.2).** Transactional entities (Sales/Purchases/Casting/Plating) have inline action buttons on their mobile cards — Pay/Bill/Return (Sales/Purchases) or Pay/Bill (Casting/Plating). Master data entities (Customers/Suppliers/Employees/Vendors) intentionally do NOT — their mobile cards show just name + phone + entity-specific aggregates (type chip + salary for employees; casting/plating counts + owed for vendors), and tapping the card opens the read-only detail modal whose Edit link routes to the form modal. **Why the asymmetry**: the workflow shape differs. Transactional entities have a "log a payment quickly mid-workflow" quick-action need; master data entities are reference rows whose edits are infrequent and benefit from the wider modal context (full address, notes, etc.) before changes. Surfacing tiny edit/delete buttons on every master-data card would also conflict with the tap-to-detail interaction (every card already has a `clickable` cursor + active state). **Reference**: `CustomerMobileCard` / `SupplierMobileCard` / `EmployeeMobileCard` / `VendorMobileCard` in their respective table files. If a future workflow surfaces a real quick-action need (e.g. "deactivate karigar" in seasonal slowdown), revisit — the prop interface is identical to the transactional cards (`onCardClick` + optional `onX` callbacks).

- **Piece entries as a flat model — no `jobType` discriminator (Phase 18).** `PieceEntry` is `{employeeId, date, count, ratePerPiece, totalAmount, note}`. The natural extension — distinct piece-rate jobs per LABOUR worker (e.g. "ear-stud assembly @₹40/piece" vs "necklace finishing @₹60/piece") — would add a `jobType` column to discriminate. **Rejected for Phase 18** because (a) the workshop owner described daily piece tracking as a single per-employee rate workflow, and (b) starting with a discriminator that has only one value forces a UX decision (default value? required selection?) that adds friction without payoff. **Extension shape if needed later**: add `jobType String?` to `PieceEntry`, migrate existing rows to a default value, extend bulk-piece-entry form with a job-type picker per row (or sticky-default at the form level), update `getOutstandingWages` if jobs ever require different payment-tracking semantics. Adding `jobType` is a single migration + a small form change; the rest of the pipeline (`computeOutstandingWages` etc.) doesn't care because outstanding aggregation is per-employee, not per-job-type. Discovered Phase 18 planning.

- **Bulk piece entry UX optimised for end-of-day workflow (Phase 18).** The `/labour` page Section 3 is a single-screen form: one row per active LABOUR employee with pre-filled rate (from `Employee.ratePerPiece`) + count input + live line-total + "Save all" button at the bottom. Zero-count rows skipped client-side AND server-side. **Rejected alternatives**: (a) per-employee modal — kills the daily-batch workflow; (b) Excel-like grid — overengineered for the typical 5–15 active LABOUR employees; (c) save-on-tab-out — accidentally creates rows on misclicks. The current shape lets the user tab through count fields, hit Save all once, and walk away. If the active LABOUR employee count grows past ~25, the single-screen layout may need to introduce a "show only with count >0" filter for readability — but at workshop scale (~5–10 active karigars), the flat list is the right shape. Discovered Phase 18 planning.

- **Unified `EmployeePayment` ledger with `type` discriminator instead of separate tables (Phase 18).** `SALARY` (FIXED employees, monthly) and `WAGE` (LABOUR employees, per-period) live in one `employee_payments` table with a `EmployeePaymentType` enum. **Rejected alternatives**: (a) separate `salary_payments` + `wage_payments` tables — fragments the per-employee payment history view (the user always wants chronological mixed-type listing); (b) a `payments` table with a polymorphic `payerType` discriminator — overgeneralises for two cases. Single table with type column is cleanest. **Type-vs-employee-type guard** lives at the action layer (`createEmployeePayment` rejects `SALARY` for LABOUR and `WAGE` for FIXED); the DB doesn't enforce this because the constraint is cross-table and Postgres CHECK constraints can't see across rows. The schema-level superRefine for `periodEnd >= periodStart` is intra-row and DOES live in the schema. Discovered Phase 18 planning.

- **Wage payment uses period-overlap calculation, not per-entry paid flag (Phase 18).** Instead of foreign-keying each `PieceEntry` to the `EmployeePayment` that paid it (which would require updating all matching entries on payment), wage coverage is computed as a function: `isPieceEntryCovered(entry, payments) = exists payment where payment.type='WAGE' AND payment.deletedAt IS NULL AND payment.periodStart <= entry.date <= payment.periodEnd`. **Why**: (a) production tracking (PieceEntry rows) stays decoupled from payment tracking (EmployeePayment rows); (b) ad-hoc payment periods are first-class — "pay the past 9 days" doesn't need to know which 9 days are in the DB; (c) soft-deleting a payment unpays everything in its window without per-row updates; (d) corrections via the existing soft-delete-and-recreate audit pattern don't accumulate stale FKs. **Trade-off**: outstanding-wages aggregation is O(entries × payments) at compute time instead of O(entries). At workshop scale (~10 active LABOUR × ~20 unpaid days × a handful of payments = ~few thousand ops) this is fine; the `@@index([employeeId, date, deletedAt])` + the in-memory `for` loops make it sub-millisecond. If the table grows to 100k+ rows per employee, denormalize via a `coveredByPaymentId` cache column with trigger-based invalidation. Not needed at current scale. Discovered Phase 18 planning.

- **Per-employee payment modal, not single-click "mark as paid" (Phase 18).** The Pay button in `/labour` Section 1 + Section 2 opens `<EmployeePaymentModal>` with amount + period + paid-date + note pre-filled — user reviews, optionally overrides, then confirms. **Rejected alternative**: single-click "mark this employee paid for current month / earliest unpaid period" that creates the payment with full defaults. **Why explicit**: the audit trail wants every payment to have a deliberate "I paid this amount on this date for this period" record. The friction of the modal is the feature — it prevents accidental click-throughs and ensures the user thinks about whether the pre-filled amount matches what they actually handed over (cash adjustments, partial payments, etc.). For SALARY payments where the amount is fixed, the modal is essentially "click → review → save"; for WAGE payments where rounding/adjustments are common, the modal is where the user keys in the actual paid amount. Discovered Phase 18 planning.

- **Tabbed completed-history view, not sub-routes or one combined list (Phase 19).** `/completed` aggregates the five entity types (Sales / Purchases / Casting / Plating / Payroll) under a single page with one tab each. **Rejected alternative — sub-routes per entity** (`/completed/sales`, `/completed/purchases`, ...): scatters the workflow ("did the work happen this month?") across five URL navigations and forces filter duplication per route. **Rejected alternative — single combined list with a Type column** (Sale / Purchase / Casting / Plating / Payment): forces a least-common-denominator row shape that loses entity-specific columns (line items for sales, materials+weight for casting, employee+period for payroll) — these are NOT cosmetically different, they're the actual point of looking at the row. Tabs let each entity render its own column set while keeping the URL and filter surface unified. **Adding a new entity type** to /completed (if some future entity supports the `completed` status): three things — extend `getCompleted<Entity>` query helper, add a tab to `completed-client.tsx`, ensure the entity has a read-only detail modal. ~30 min. Discovered Phase 19 planning.

- **Shared filters across all tabs, not per-tab (Phase 19).** Date range (from/to) + party-or-employee search apply to **every** tab simultaneously — changing the range on the Sales tab updates Purchases/Casting/Plating/Payroll too. **Rejected alternative — per-tab filter state** (each tab remembers its own date range): mismatches the user's mental model ("show me everything that happened in October"). The workshop user thinks calendar-window-first, then drills into entity types within that window. **Implementation**: client component owns from/to/q state, `router.replace`s the URL on filter change; the server page parses search params and re-runs ALL five query helpers with the same filter. Tab switches preserve the filter because the URL stays the same — only the active panel changes locally. Discovered Phase 19 planning.

- **Strict status===completed filter for transactional entities; Payroll shows every payment (Phase 19).** Sales/Purchases/Casting/Plating filter rows to `status === 'completed'` (post-`computeTransactionStatus`); `pending`/`partial`/`refund_due` rows are excluded because they're still operationally in-flight — the user works those on the entity-specific page (e.g., `/sales`). Payroll is different: every non-deleted `EmployeePayment` is **inherently** a completed event ("the payment happened, the money moved"). There's no concept of a "partially paid" payment row. So the Payroll tab applies date+employee filters but no status filter. **This asymmetry is correct, not a bug** — the underlying business semantics differ. If a future entity has the same inherently-completed shape (e.g., a hypothetical immutable journal entry), it follows Payroll's pattern. Discovered Phase 19 planning.

- **Dedicated read-only tables per tab, not the entity list tables (Phase 19).** The four transactional list tables (`sales-table.tsx`, `purchases-table.tsx`, `casting-table.tsx`, `plating-table.tsx`) bake in mutation UX (inline Pay/Bill/Return action buttons, Edit/Delete row hover actions, internal search input + Add button at the top, internal detail-modal state). **None of those affordances belong on /completed** — completed transactions are settled; adding a Pay button on an already-paid row would be cognitive noise, and the Add button doesn't make sense on an aggregated view. The shared search input also conflicts with /completed's cross-tab unified filter bar. **Decision**: build dedicated read-only tables per entity in `src/app/(app)/completed/completed-*-table.tsx` (Sales, Purchases, Casting, Plating, Payroll). They reuse `ResponsiveTable`, `TransactionStatusChip`, format helpers, and the existing read-only detail modals (which are already mutation-free post-Phase 10). **Rejected alternative — refactor the existing list tables to accept a `mode: 'view' | 'manage'` prop**: would have added prop-driven conditional rendering across ~200 LOC × 4 tables for the benefit of avoiding ~150 LOC of dedicated tables × 4. Net LOC is similar; the dedicated tables are simpler to read because they're scoped to one job. Discovered Phase 19 build.

- **Payroll rows non-clickable on this iteration (Phase 19).** The Payroll tab shows EmployeePayment rows with employee name + type + period + amount + note + paid-on all visible inline. Rows do not open a detail modal. **Why**: there's no `EmployeePaymentDetailModal` in the codebase yet, and reusing the `EmployeeDetailModal` (which fetches full history) is heavier than the workflow needs. Inline-visible fields cover what the user wants when scanning the payroll log. **If row-click → modal proves valuable** in real usage (e.g., user wants to see the note in full, or jump to the employee), the implementation shape is straightforward: build a `<EmployeePaymentDetailModal>` that takes the `EmployeePaymentForCompleted` row + opens with the employee name as the title + shows the period / type / amount / note in full + has a "View employee" link to the employee detail. ~30 min to build when the need surfaces. Discovered Phase 19 planning.

- **Kaccha-bill model — no GST, HSN codes, or tax handling.** The business operates on informal "kaccha" (literally "raw" / unofficial) bills — the workshop's invoicing tradition, common across small Mumbai jewelry manufacturers. The schema intentionally excludes every tax-related field: no `gstin` on `Customer` / `Supplier` / `CastingPlatingVendor`, no `gstAmount` / `cgst` / `sgst` / `igst` on `Sale` / `Purchase` / `CastingEntry` / `PlatingEntry`, no `hsnCode` on `SaleLineItem` / `PurchaseLineItem`. Invoice math is plain rupees throughout: `total = SUM(line.qty × line.rate) − discount`, no tax addition layer. The four-state status machine (`pending` / `partial` / `completed` / `refund_due`) reflects payment + return state only, not tax-filing state. **Why intentional**: the workshop's customers expect kaccha bills, the workshop's books are kept on the same basis, GST registration is not required at this transaction volume + business structure. Adding tax fields proactively would (a) clutter every form with fields the user would leave blank, (b) introduce display ambiguity ("is this total including or excluding tax?"), (c) suggest the app supports a workflow it doesn't (filing returns, generating GSTR-1, etc.). **If GST adoption ever becomes required** (business restructure, threshold crossed, customer demands), the migration shape is well-understood: add nullable `gstin` to parties, add `gstAmount` / `cgst` / `sgst` (BigInt paise) to transactions, add `hsnCode` to line items, add a tax-inclusive vs tax-exclusive toggle to the invoice math, build a returns-export view. Estimated effort: ~3–4 phases. **Not a deferred item — explicit non-feature.** This entry exists to prevent future contributors from "fixing" the missing-GST appearance and to anchor the schema's plain-rupees invariant. Discovered as polish-session documentation gap; no implementation change.

## Resolved (one audit cycle)

Items completed but kept here for one audit cycle for traceability, then pruned.

- **Phase 19 — Completed transactions page** — *[resolved on 2026-05-19]*. ADMIN-only `/completed` route aggregates settled history across all five entity types (Sales / Purchases / Casting / Plating / Payroll) under tabs with shared filters (date range + party-or-employee search) defaulting to the current IST calendar month. **No DB migration** — purely a read-only query layer on top of existing tables. **New files**: `src/lib/completed-queries.ts` (5 query helpers, each pushing date range + party search into Prisma and post-filtering by derived status); `src/components/ui/tabs.tsx` (minimal Radix-Tabs primitive matching the project's flat-and-sharp design tokens); `src/app/(app)/completed/page.tsx` (server component, role-redirect, parallel fetch); `src/app/(app)/completed/completed-client.tsx` (tab state + filter URL sync via `router.replace`, debounced query 300ms); five dedicated read-only tables (`completed-{sales,purchases,casting,plating,payroll}-table.tsx`). **Existing files modified**: `role-access.ts` (+ `canViewCompleted` / `canViewReports`), `proxy.ts` (`/completed` + `/reports` gated to ADMIN), `sidebar.tsx` (flip `/completed` to `enabled: true`; `/reports` stays "Soon" — Phase 15 territory). **Defense-in-depth**: proxy URL gate + page server-component redirect + sidebar conditional filter; all three layers verified by Step 12 of the production walkthrough (non-ADMIN URL hack → /dashboard, sidebar excludes the nav item). **Scope**: 13 files changed (+1676 / −1 LOC). **Test count**: 1224 → 1264 (+40 net new tests across `completed-queries.test.ts`, `role-access.test.ts` (extended), `completed/page.test.tsx`, `completed/completed-client.test.tsx`). `npx tsc --noEmit` clean; production walkthrough `scripts/walkthrough-p19-completed.mjs` 12/12 PASS. **Notable test-craft discovery**: Radix Tabs trigger selection needs `userEvent.click` (pointerdown + click sequence), not `fireEvent.click` — `fireEvent.click` fires a synthetic click but doesn't propagate through Radix's pointer-event-based selection logic, so the active tab doesn't change in jsdom. Documented in `docs/TESTING.md`. **Plan deviation logged**: Payroll rows are non-clickable on this iteration — the user's original plan called for "opens employee detail modal with focus on the payment row," but the existing EmployeeDetailModal fetches full history (Pieces + Payments) and is heavier than the workflow needs; inline-visible row fields cover the use case. A future lightweight `EmployeePaymentDetailModal` is a ~30-min lift if needed. **Next audit cycle: prune.**

- **Phase 18 — Labour management upgrade** — *[resolved on 2026-05-19]*. Operational labour management built on top of the existing Employee master. Two new tables (`piece_entries`, `employee_payments`) + one new enum (`EmployeePaymentType`) + one new column (`employees.ratePerPiece` BigInt? paise). Migration `20260519145247_labour_management` is additive-only — no data motion, safe to apply against any existing prod state. **`/labour` route** (ADMIN + LABOUR_MGMT) renders three sections: Pending salaries (FIXED employees missing current-month payment) / Outstanding wages (LABOUR employees with PieceEntries not covered by any WAGE payment's period) / Bulk piece entry (one row per active LABOUR employee with count + rate inputs). **Wage coverage by period overlap, NOT per-entry paid flag** — `isPieceEntryCovered(entry, payments)` returns true iff any non-deleted WAGE payment's `[periodStart, periodEnd]` contains entry.date inclusively. Decouples production tracking from payment tracking; ad-hoc payment periods supported. **`labour-balances.ts` pipeline** mirrors `outstanding-balances.ts` shape: pure functions (`computeOutstandingWages`, `isPieceEntryCovered`, `isMonthSalaryPaid`) + DB aggregators (`listEmployeesWithOutstandingWages`, `listEmployeesMissingSalaryThisMonth`, `getOutstandingWages`, `getLabourSummary`, `countPieceEntriesForIstDay`). **IST-aware month helpers** added to `src/lib/format.ts`: `startOfMonthIST`, `endOfMonthIST`, `formatMonthIST`, `monthIsoIST`, `currentIstYearMonth`, plus current-month convenience variants. All period calculations pin to `Asia/Kolkata`; no browser-local-time leakage (the polish-session SP1 hydration-drift fix's precedent). **`<EmployeePaymentModal>`** — single-employee payment modal with SALARY/WAGE type chip, amount/period pre-fill from caller defaults. **Role-scope**: `canViewLabour(role)` + `canManageLabour(role)` in `src/lib/role-access.ts`; `/labour` in proxy `ROUTE_ROLES`; every `labour/actions.ts` server action gates via `requireRole(['ADMIN','LABOUR_MGMT'])`. **Employee detail modal** extended with Pieces history (LABOUR only) + Payment history sections; data fetched via new `getEmployeeHistory(employeeId)` action. **Dashboard** gets labour section for ADMIN + LABOUR_MGMT (Salaries due / Outstanding wages / [Pieces today on LABOUR_MGMT only]). **Sidebar** Labour nav item with `HardHat` icon (ADMIN + LABOUR_MGMT visible). **Scope**: 18 files modified, 12 files created, 1 migration. 1224 tests (+115 from 1109); `tsc --noEmit` clean. Production walkthrough deferred to user post-deploy via `scripts/walkthrough-p18.mjs`. **Originally-motivating use case from the project owner — "the labour management workflow needs to be done first-class" — now answered by piece-rate ledger + monthly salary reminders + period-overlap wage tracking.** **Next audit cycle: prune.**

- **Phase 17a — Party unification** — *[resolved on 2026-05-19]*. Customer / Supplier / CastingPlatingVendor consolidated into a single `Party` table with role flags (`isCustomer`, `isSupplier`, `isCastingVendor`, `isPlatingVendor`); phone globally unique; transactional FKs renamed (`Sale.customerId` / `Purchase.supplierId` / `CastingEntry.vendorId` / `PlatingEntry.vendorId` → `partyId`). Four per-entity pickers consolidated into a shared `<PartyPicker>` at `src/components/party-picker.tsx` parameterised by `role` + `inputIdPrefix`. Master data folders (`customers/`, `suppliers/`, `vendors/`) preserve their routes; their actions wrap `prisma.party` with role filters and the phone-collision dedup flips role flags on existing parties rather than duplicating. Walk-in auto-promotion implicitly sets the role flag based on form context. Migration `20260519150000_unify_party_model` applied to dev + prod (the 1 active prod customer "hitesh" migrated cleanly to a Party with `isCustomer: true`). **Scope**: 93 files changed, +1428/−2074 LOC (net −646 LOC). 1064/1064 tests passing; `tsc --noEmit` clean. Phase 17b (Payables/Receivables UI) now unblocked — cross-table party rollups are a clean `prisma.party.findMany({ include: { sales: …, purchases: …, castingEntries: …, platingEntries: … } })` query against the unified table. **Next audit cycle: prune.**

- **Phase 17b — Payables/Receivables UI** — *[resolved on 2026-05-19]*. Two new routes (`/payables`, `/receivables`) plus a sub-route per party (`/payables/[partyId]`, `/receivables/[partyId]`); new bulk-allocation `<PartyPaymentModal>` (`src/components/action-modals/party-payment-modal.tsx`) creates N payments across heterogeneous entity types in one `prisma.$transaction` for atomicity. Outstanding-balance pipeline in `src/lib/outstanding-balances.ts` (pure `computeOutstanding` + four party-level aggregators with single-query bulk attachment lookups, no N+1). Role-access matrix in `src/lib/role-access.ts` (`canViewPayables(role, scope)` / `canViewReceivables(role)` / `effectivePayableScope(role)`); three-layer enforcement (proxy `ROUTE_ROLES`, page server-component redirect, server-action role-intersection gate). Dashboard gets per-role payables/receivables cards + top-3 party lists. Missing-attachment alerts surface on list rows + per-transaction rows in the modal. Phase 17a deferred UX resolved: `<PartyRoleChips>` (`src/components/party-role-chips.tsx`) renders one chip per active role flag in customer/supplier/vendor detail modals. **Scope**: 28 files changed, +3262/−87 LOC. 1109 tests (+45 from 1064); `tsc --noEmit` clean. Production walkthrough: 25/25 PASS (`scripts/walkthrough-p17b-payables.mjs`). **Originally-motivating use case from the project owner — "what do I need to pay today?" — now answered by the app, not memory.** **Next audit cycle: prune.**

- **Phase 17a deferred — Party detail modal role-chips** — *[resolved on 2026-05-19]* in Phase 17b. `<PartyRoleChips>` wired into customer/supplier/vendor detail modals; multi-role parties show all active role chips. **Next audit cycle: prune.**

- **Production soft-delete cleanup — day-0 baseline (polish session sub-phase 3 follow-up)** — *[resolved on 2026-05-19]*. The SP3 commit surfaced that the earlier polish-session soft-delete cleanup ran against dev, not prod (Supabase MCP was dev-connected). Real prod still held the user's accumulated test data from production exploration: 3 soft-deleted customers (`hdfn`, `darshan`, `adarsh`), 2 soft-deleted suppliers (both `darshan`), 4 soft-deleted sales (`dArshan`, `darshan`, `adarsh` ×2 — the original `dArshan/sdf` names the polish-session brief flagged), 3 soft-deleted purchases (`sdf`, `darshan` ×2), 1 soft-deleted casting entry (`xyz`), 1 soft-deleted casting/plating vendor (`xyz`), and 11 fully-soft-deleted attachment rows.

  **Cleanup tool**: `scripts/_cleanup-prod-real.mjs` (gitignored under `scripts/_*.mjs`). Differences from the earlier `_cleanup-prod-soft-deletes.mjs`: (a) opens a direct `pg` connection to prod via `DIRECT_URL` from `.env.production.local` rather than going through the (dev-pointed) Supabase MCP; (b) explicit `projectRef === 'cseqdcrfnvgsalsyhjsz'` safety check that aborts if pointed anywhere else; (c) discovers the attachment list dynamically from the DB instead of hardcoded IDs (so future re-runs adapt to the current state); (d) sweeps the R2 objects with HEAD-before-DELETE and tolerates 404s; (e) executes all 8 DB DELETEs in FK-safe order (attachments → sales → purchases → casting_entries → plating_entries → customers → suppliers → casting_plating_vendors); (f) idempotent — re-running produces zero ops.

  **R2 state**: all 11 prod attachment R2 keys were already absent (HEAD → 404), confirming `softDeleteBill` / `softDeleteAttachment`'s "R2 first, DB tombstone second" sequence worked correctly during the user's production exploration too. No R2 bucket bloat to clean.

  **Final prod state** (verified via direct `pg` query at the closing pass): 0 active OR soft-deleted rows in every business table — `customers`, `suppliers`, `employees`, `casting_plating_vendors`, `sales`, `purchases`, `casting_entries`, `plating_entries`, `attachments`, and every cascading child table. Only `users` retains content (4 rows: 1 ADMIN + 3 RBAC test accounts per HANDOFF.md § 8). Production is day-0 ready for first real business data.

  **Onboarding rule reinforced**: before running any cleanup SQL, verify which DB the connection points at — `SELECT email FROM users LIMIT 1` against prod returns `c.darshan.somaiya369@gmail.com`; dev returns `kirtithakkar@shreecreation.com`. The polish-session mix-up could have been caught in 5 seconds with that one query. `_cleanup-prod-real.mjs` now hardcodes the prod project-ref check at the top. **Next audit cycle: prune.**

- **R2 token de-escalated from Admin Read & Write → Object Read & Write (polish session)** — *[resolved on 2026-05-19]*. Phase 8 left the prod R2 credentials scoped to **Admin Read & Write** because the one-time `PutBucketCors` call needed Admin-level permissions; CORS rules persist independent of the credentials that set them, so the elevated scope was always meant to be temporary. The polish session rotated to a least-privilege **Object Read & Write** token scoped to the specific bucket (`jewelrytracker`). **Verification methodology**: a new `scripts/_smoke-r2-token.mjs` (gitignored under `scripts/_*.mjs`) exercises every S3 operation the prod app actually performs against the live prod bucket — `getSignedUrl(PutObjectCommand)`, `fetch() PUT` to the presigned URL, `HeadObjectCommand` (the `confirmUpload` mime+size check), `getSignedUrl(GetObjectCommand)`, `fetch() GET` body verification, `DeleteObjectCommand`, and a 404-confirm `HeadObjectCommand`. Ran 3 times: (a) baseline with old Admin token → 7/7 pass; (b) post-rotation with new Object R&W token, old still active → 7/7 pass; (c) old Admin token revoked + 30s cache flush → 7/7 pass. Vercel prod env vars (`R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`) updated via `scripts/_set-r2-env.mjs` (idempotent upsert against all 5 R2 keys); redeploy `dpl_CuUwTWwA89hedcbuPQkC4Z5iYDTn` reached READY in 62s; prod URL responds 200 on `/auth/login` throughout. **Onboarding note** — keep `_smoke-r2-token.mjs` for future rotations: 7-op coverage of the app's actual R2 surface in <2 seconds beats clicking through the BillActionModal UI. **General pattern**: when an elevated permission was granted for a one-off task and the artifact (CORS rules, etc.) persists past the credential, schedule the de-escalation as a separate sub-phase rather than leaving the elevated credentials in place indefinitely. **Next audit cycle: prune.**

- **Bill → Attachment rename (polish session sub-phase 3)** — *[resolved on 2026-05-19]*. The `Bill` table — Phase 8's invoice-attachment store — became semantically misleading when Phase 12a added photo support via the `PURCHASE_PHOTO` discriminator. The polish session renamed the entire data-layer surface for clarity: model `Bill` → `Attachment`, table `bills` → `attachments`, enum `BillStatus` → `AttachmentStatus`, FK columns `casting_entries.billId` / `plating_entries.billId` → `attachmentId`, plus all indexes/constraints renamed to follow the Prisma naming convention.

  **Code scope**: ~56 source files touched. Identifier renames (model refs, function names, component names, type names, prop names) via `scripts/_rename-bill-to-attachment.mjs` (gitignored) — a tokenizing rewriter that handles code identifiers while preserving string-literal UI text. Specific renames: `prisma.bill` → `prisma.attachment`; `Bill` type → `Attachment`; `softDeleteBill` → `softDeleteAttachment`; `getBillViewUrl` → `getAttachmentViewUrl`; `getBillForEntity` → `getAttachmentForEntity`; `attach/detachBill*` → `attach/detachAttachment*`; `canAccessBill` → `canAccessAttachment`; `BillPreview` → `AttachmentPreview`; `BillActionModal` → `AttachmentActionModal`; `uploadedBills` → `uploadedAttachments` (User relation). File renames via `git mv`: `bill-preview.{tsx,test.tsx}` → `attachment-preview.{tsx,test.tsx}`; `bill-action-modal.{tsx,test.tsx}` → `attachment-action-modal.{tsx,test.tsx}`; `bill-access.{ts,test.ts}` → `attachment-access.{ts,test.ts}`; folder `src/app/(app)/bills/` → `src/app/(app)/attachments/`. Two follow-up Node scripts (`_rename-bill-to-attachment.mjs` for code, `_rename-bill-paths.mjs` for import paths) plus a few targeted manual edits for the cases the tokenizer couldn't reach (Prisma `include: { bill: true }` relation keys, RHF `register("billId")` string paths, and `const bill = await tx.bill.findUnique(...)` variable declarations).

  **Preserved (user-facing)**: every UI string with "bill" / "Bill" — "Add bill", "Attach bill", "View bill", "Replace bill", "Bill section" headings, status chip labels, error messages like `"Bill not found"`. The workshop user calls these "bills" regardless of the internal data-layer name. Also preserved: discriminator string values (`SALE` / `PURCHASE` / `CASTING_ENTRY` / `PLATING_ENTRY` / `PURCHASE_PHOTO` — these are values inside the `attachedToType` column, not model names); the admin sandbox URL `/admin/bills-test` (admin-facing route name); historical script filenames like `walkthrough-p8-bills.mjs`.

  **Migration**: `prisma/migrations/20260519144601_rename_bill_to_attachment/migration.sql` — pure DDL renames (`ALTER TYPE RENAME`, `ALTER TABLE RENAME TO`, `ALTER INDEX RENAME`, `ALTER TABLE RENAME COLUMN`, `ALTER TABLE RENAME CONSTRAINT`). No data motion because PostgreSQL renames are in-place metadata operations. Applied to dev DB via `npx prisma migrate deploy`; applied to prod DB via new `scripts/_migrate-prod.mjs` (gitignored — spawns `prisma migrate deploy` with prod env vars overriding the `.env.local` defaults that `prisma.config.ts` loads).

  **Verification**: `npx tsc --noEmit` clean across all 56 files; 1096/1096 tests pass; prod migration confirmed via direct `pg` connection (`attachments` table exists, `bills` table gone, all 4 prod users intact, all 17 migrations recorded).

  **Onboarding notes**: (1) When adding a second multi-attachment use case (e.g., `SALE_PHOTO`, `VENDOR_EVIDENCE_PHOTO`), the polymorphic discriminator pattern stays — just add a value to `ATTACHED_TO_TYPES` and a row to `ROLE_MATRIX` in `attachment-access.ts`. No schema change required. (2) The tokenizing rewriter pattern in `_rename-bill-to-attachment.mjs` is reusable for any future bulk identifier rename — it skips string literals and template-literal text content but processes the code spans, plus handles JSX expression syntax via the same tokenizer. (3) `scripts/_migrate-prod.mjs` is the documented path for prod migration application — Vercel build does NOT run `prisma migrate deploy`. **Next audit cycle: prune.**

- **Development DB soft-delete cleanup (polish session)** — *[resolved on 2026-05-19]*. Two-pass cleanup of historical walkthrough artifacts in **dev** DB (`rylgkzatxebvmemwspyb`). **Note**: an earlier draft of this entry mistakenly described the cleanup as "production" — the Supabase MCP I had access to was actually pointed at dev, not prod. The cleanup itself was correct housekeeping for dev (which had accumulated 1 soft-deleted supplier + 15 soft-deleted sales + 2 soft-deleted purchases + 11 soft-deleted bills — all walkthrough artifacts). Production has its own separate set of soft-deleted rows from the user's testing — those are addressed in a separate `Production data cleanup (polish session sub-phase 3 follow-up)` entry below.

  **Pass 1 — Named-target audit + orphan supplier:** Audited dev for the historically-referenced typo-named rows (`fzd`, `dfg`, `dArshan`, `sdf` — surfaced in the Phase 7 institutional-memory entry as the first sign of multi-item-invoice need). Zero matches in `customers`, `suppliers`, `sales.partyName`/`partyPhone`, or `purchases.partyName`/`partyPhone` on dev — already absent, likely scrubbed during Phase 7's line-items migration cleanup. Audit then swept `name LIKE '__phase%'` / `'__walkthrough%'` and surfaced one stray active row: `suppliers.cmpa75xgp000cc0v09x0yzsiw` named `__phase10_6walk_purchaseparty`, an orphan auto-promoted during Phase 10.6 walkthrough's walk-in flow that was never scrubbed at close. Confirmed zero active FK refs in purchases. Hard-deleted.

  **Pass 2 — Comprehensive soft-delete sweep (dev):** Audit then revealed dev had been accumulating soft-deleted walkthrough artifacts across multiple tables — 1 supplier, 15 sales (with 2 sale_payments + 1 sale_return as cascading children + line items), 2 purchases (with cascading children), and 11 fully-soft-deleted bill rows. None were business data; all were `Pmt3.3 A/B …`, `xyz`, `Edge3Walkin`, `Walkthrough Buyer`, `__phase*`, `Darshan Somaiya` test sale, etc. **Order of cleanup** (matters because of discriminator-not-FK + onDelete:Cascade for FK children): (a) ran `scripts/_cleanup-prod-soft-deletes.mjs` (gitignored — note the misleading filename: it ran against dev because the Supabase MCP that the script's hardcoded ID list came from was dev-connected) — for each of the 11 soft-deleted bills, attempted R2 HEAD via the new Object R&W token, found all 11 R2 objects already absent (confirmed `softDeleteBill`'s sequence — delete R2 first, then tombstone DB — had worked correctly during prior walkthroughs); (b) `DELETE FROM bills WHERE deletedAt IS NOT NULL` (11 rows); (c) `DELETE FROM sales WHERE deletedAt IS NOT NULL` (15 rows; cascaded to sale_line_items / sale_payments / sale_returns); (d) `DELETE FROM purchases WHERE deletedAt IS NOT NULL` (2 rows; cascaded similarly); (e) `DELETE FROM suppliers WHERE deletedAt IS NOT NULL` (1 row).

  **Final dev state**: 1 active customer (Darshan Somaiya), 0 suppliers, 4 active employees (test karigars/salaried — intentional retention for testing), 0 vendors, 0 sales / purchases / casting / plating, 0 bills, 0 R2 objects under `bills/`. Dev DB is back to a clean slate.

  **Onboarding notes**: (1) future walkthrough scripts that exercise walk-in auto-promotion MUST scrub the auto-created Customer / Supplier / Vendor row in their cleanup step — not just the transactional rows. The `__phase*walk_*` LIKE pattern catches them. (2) When hard-deleting rows that reference Attachment via the discriminator (no FK), run the R2 HEAD-or-DELETE sweep BEFORE the DB DELETE; the cascade won't reach the attachments table. (3) FK cascades worked correctly for line items / payments / returns — deleting the parent removed all children cleanly. (4) **Always verify which DB the Supabase MCP is pointed at before running cleanup SQL** — query a known-distinct row (e.g., admin email) to disambiguate dev vs prod. The Phase 4.5 deployment doc captures the dev / prod admin email split. **Next audit cycle: prune.**

- **Sale payments + payment-aware status (Phase 3.2)** — *[resolved on 2026-05-14]*. `SalePayment` child table built with `saleId` FK + `onDelete: Cascade`, soft-delete via `deletedAt`, BigInt paise `amount` column. Aggregation-based `paidAmount` (no cached column) computed in `serializeSale()` via `payments.reduce(... amount, 0n)` over Prisma `include`. Status branches pending/partial/completed are live; refund_due remains dormant pending Phase 3.3 returns. Inline payment panel inside `sale-detail-modal` with history list, outstanding balance, expandable form, "Pay full balance" auto-fill, action-layer overpayment block with formatted error, and × → Confirm soft-delete with status recomputation. Live-updating modal pattern (viewingSaleId derivation) introduced to keep the chip in sync after `router.refresh()` without close-and-reopen. **Next audit cycle: prune.**

- **Sale returns + `refund_due` status (Phase 3.3)** — *[resolved on 2026-05-14]*. `SaleReturn` child table built with `saleId` FK + `onDelete: Cascade`, soft-delete via `deletedAt`, `qtyReturned` int + `refundAmount` BigInt paise. `SalePayment` extended with `PaymentType` enum (`PAYMENT | REFUND`, default `PAYMENT`) + `@@index([saleId, type])` for typed aggregation; existing 3 payment rows backfilled to `PAYMENT` cleanly. `computeSaleStatus` rewritten with explicit thresholds — `effectiveTotal = total − returnTotal`, `refund_due` now active when `paidAmount > effectiveTotal`. `serializeSale` extended with `returnTotal` + `returns` + net `paidAmount` (PAYMENT minus REFUND). New inline Returns panel in detail modal alongside Payments panel (separate sections, both live-update via `viewingSaleId`). PaymentPanel extended with refund-mode UI: button label flips "Record payment" ↔ "Issue refund", autofill flips "Pay full balance" ↔ "Refund full amount", indicator flips "Outstanding" ↔ "Refund owed" (red), REFUND entries render with red `Refund` badge + minus-prefixed amount + `text-error`. Status chip's `refund_due` state distinct from blue partial/completed via `text-error` on label + red dot. Action-layer guards: cumulative qty ≤ sale.qty, cumulative refund ≤ sale.total, refund amount ≤ net paidAmount. Sales transaction model is structurally complete; only Phase 3.4 receipts remain. **Next audit cycle: prune.**

- **Phase 4 Purchases** — *[resolved on 2026-05-14]*. `Purchase`, `PurchasePayment`, `PurchaseReturn` models built as structural mirror of Sales — same schema shapes with `customerId → supplierId`, `saleId → purchaseId`. PaymentType enum reused (single definition serves both `SalePayment` and `PurchasePayment`). `src/lib/sale-status.ts` renamed to `src/lib/transaction-status.ts` with unified `computeTransactionStatus` + `TransactionStatus` type; `SaleStatus` and `PurchaseStatus` retained as call-site type aliases. `<TransactionStatusChip>` moved to `src/components/` (shared). UI label inversions wired into `purchases/payment-panel.tsx` and `purchases/return-panel.tsx`: "Outstanding" → "Owed to supplier", "Refund owed" → "Refund expected", "+ Issue refund" → "+ Record refund received", REFUND-row red/`−` → blue/`+`. Walkthrough verified 13/13 (full lifecycle pending → partial → completed → refund_due → completed) with explicit DOM-level dual assertions on the inverted labels. 172 new tests cloned from Sales suite bring totals to 496. Sales + Purchases transaction subtrees are now structurally complete. **Phase 3.4 receipts deferred indefinitely** (no current user demand; the Sales/Purchases sub-track stable as is). **Next audit cycle: prune.**

- **Phase 4.5 production deployment** — *[resolved on 2026-05-15]*. App live on Vercel at `https://jewlerytracker-darshan-somaiyas-projects.vercel.app`. Vercel project `prj_G044SI4W3lUfumtIGuAvzYDOhABA` (framework auto-detected `nextjs`, node 24.x, Fluid Compute, region iad1) feeding from GitHub `Darshan36/jewelry-tracker` `main` branch. Supabase prod project `cseqdcrfnvgsalsyhjsz` (Mumbai ap-south-1, free tier) created with all 10 migrations applied via `prisma migrate deploy`, anon/authenticated grants revoked, single ADMIN user `c.darshan.somaiya369@gmail.com` seeded. Vercel SSO Deployment Protection disabled (the app does its own auth). Operational reference in `docs/HANDOFF.md`. Two deployment-related bugs surfaced and fixed:
  - **Lazy Prisma client construction (`src/lib/prisma.ts`).** Eager `if (!process.env.DATABASE_URL) throw` at module load broke Next.js 16's build-time page-data collection — the route-handler module load fired the throw before env-var resolution completed. Refactored to a Proxy that defers all construction to first property access. The throw still fires on a genuinely missing env var; it just no longer triggers during build static analysis. See `src/lib/prisma.ts` for the canonical pattern.
  - **`authorize()` diagnostic wrap (`src/lib/auth.ts`).** Auth.js v5 wraps exceptions thrown inside `authorize` as `CallbackRouteError` and redirects to `?error=Configuration`, hiding the underlying cause. Added a try/catch wrapper that `console.error`s the real exception before rethrowing. Without this, the original `Configuration` error was indistinguishable from missing AUTH_SECRET, invalid CSRF, or DB unreachable — the wrapper made the root cause (empty `DATABASE_URL` env var, see below) diagnosable in two minutes instead of an hour.
  - **Env-var write corruption.** Initial `DATABASE_URL` upload via the Vercel REST API got truncated because the value (containing `&connection_limit=1`) was sourced through a shell `set -a; source .env.production.local` chain that treated the `&` as a background operator. The stored value silently became an empty string. Detection required a temporary `/api/debug-env` route that returned env-key presence + lengths (since deleted). Re-uploaded with the full value via Node-direct file read; functional verification followed. **Lesson:** never `source` or `set -a` `.env*` files containing URL query strings — use Node's file-read path instead. Added to KNOWN_GAPS onboarding notes and `docs/HANDOFF.md` § 5.
  - End-to-end browser walkthrough (`scripts/walkthrough-prod.mjs`) verifies login → customer → sale → payment → return → sign-out flow with cleanup. Six steps PASS, zero JS errors, zero pageerrors. Script is committed for future regression runs against new deploys.
  - **Next audit cycle: prune.**

- **CASTING_PLATING_MGMT placeholder dashboard (Phase 9)** — *[resolved on 2026-05-17]*. The `CASTING_PLATING_MGMT` role had a placeholder dashboard (single "Coming soon" card) for four phases (Phase 5 RBAC introduction → Phase 8 Bills closeout). Phase 9 replaced it with 4 real cards: Casting (this month) — count + total payable in rupees; Plating (this month) — count + total payable; Total owed — sum of remaining balance across all open casting + plating entries; Vendors — count of non-deleted `CastingPlatingVendor` rows. The dashboard branch is now `async` (mirrors `AdminDashboard` / `PurchaseDashboard` shape) and fetches via `prisma.castingEntry.aggregate` + `prisma.platingEntry.aggregate` + a `sumOwedToCastingPlatingVendors()` helper. Verified during the production walkthrough — Step 15a passed all four cards. Role finally has a functional dashboard alongside the three workflow pages. **Next audit cycle: prune.**

- **Multi-item sales and purchases (Phase 7)** — *[resolved on 2026-05-16]*. `SaleLineItem` and `PurchaseLineItem` child tables added with `onDelete: Cascade`, indexed on parent FK. Two migrations: `20260515230427_add_line_item_tables` (creates tables + hand-written `INSERT … SELECT` that copies existing `Sale.qty/rate/itemDescription` into the new tables, one line per parent) and `20260515230703_drop_sale_purchase_inline_item_columns` (drops the three legacy columns). Both applied to dev and production. Sale-level discount kept on parent; line items carry only description/qty/rate. **Phase 3.1's single-item lock is officially superseded** — that decision-lineage entry stays in place documenting why the lock was made and why it had to be reversed (institutional-memory entry above). `updateSale` / `updatePurchase` use the replace-all pattern (`deleteMany` + nested `create` inside `$transaction`). Form modals rebuilt around `useFieldArray` (one row on open, `+ Add line` appends, `×` removes — disabled on the sole remaining line). Detail modals show a read-only line-items table + Subtotal/Discount/Total rows. Sales/Purchases tables' "Items" column shows `"first description + N more"` summary; global filter searches across all line-item descriptions. Return-side guards aggregate `SUM(lineItems.qty)` instead of the removed `parent.qty`. **656 tests passing** (up from 620; 144 single-item tests rewritten + 12 new `useFieldArray` component tests across two new files + new multi-line aggregation coverage in return-actions). 12-step production walkthrough verified end-to-end. **Next audit cycle: prune.**

## Deferred items

Work intentionally not done, with revisit triggers.

- **`@hono/node-server <1.19.13` middleware bypass via repeated slashes** *(moderate, GHSA-92pp-h63x-v22m)*. Pulled transitively by `@prisma/dev → prisma`. `npm audit fix --force` would downgrade Prisma to 6.19.3 (breaking). Dev-time-only attack surface. **Revisit on next Prisma major bump** (post-7.x) — should drop automatically.

- **`postcss <8.5.10` XSS in CSS stringify** *(moderate, GHSA-qx2v-qp2m-jg93)*. Pulled transitively by `next`. Fix would downgrade `next` to 9.3.3 (massive breaking change). Build-time-only attack surface for our internal app. **Revisit on next Next.js major bump** (post-16.x) — should drop automatically.

- **Supabase Agent Skills package (`supabase/agent-skills`) not installed.** Deferred to avoid pulling Claude Code toward Supabase-native patterns (Edge Functions, RLS-first design, Supabase CLI migrations) when we've committed to Prisma + Postgres patterns instead. The Supabase MCP server alone provides enough context for read/write operations. **Revisit if MCP-only context proves insufficient** during database phases (Phase 2+).

- **Admin password rotation deferred.** Supabase keys + DB password + `AUTH_SECRET` were rotated end of Phase 2.1 (see Resolved section above), but `users.passwordHash` for the **dev** seeded admin (`rylgkzatxebvmemwspyb` project) still corresponds to the original password that appeared in the chat transcript during Phase 1.6 Task 2. Exposure surface is narrower than the Supabase keys — an attack also requires the admin email + access to the running app + going through the login flow (no direct DB read possible from the transcript-leaked value). But the leak is still real. **Revisit when `Settings → Change Password` UI is built** (Phase 6 or later), or sooner if there's external reason to believe the password has spread beyond the transcript. Direct-SQL `UPDATE users SET "passwordHash" = …` was considered for the rotation pass but rejected — the bcrypt-hash dance + direct DB UPDATE was judged higher risk than the remaining surface. `SEED_ADMIN_PASSWORD` in `.env.local` also still has the original value (kept in sync with the DB record so `npx prisma db seed` remains idempotent).

  **Update 2026-05-15**: the **prod** admin password was rotated post-Phase 4.5 via direct DB UPDATE + matching Vercel env var update + matching `.env.production.local` update — bcrypt-cost-12 hash applied, login flow re-verified end-to-end (new password authenticates, old rejected). Underlying transcript-exposure pattern persists for the new value too: until a `Settings → Change Password` UI exists, each rotation has to go through a session that observes the plaintext. Plan to rotate again via the UI once it lands; for now the value lives in `.env.production.local` (gitignored), the Vercel encrypted env-var store, and the bcrypt hash on the DB row.

- **Invite user flow not built.** Currently only the seeded admin exists. The product expects admins to create additional staff accounts (and other admins) from inside the app via an authenticated "Invite User" flow, but Phase 1.6 only seeds the single primary admin. Two more admins (Sachin and Kunj Thakkar — credentials discarded from working memory) wait for this flow. **Revisit in Phase 2 alongside master data, or later if the company doesn't need additional users yet.** Until then, ad-hoc admin creation requires running the seed script with different env vars or inserting `users` rows directly.

- **No password reset flow.** A user who forgets their password has no self-service recovery path. Admin must update the DB row (`UPDATE users SET "passwordHash" = …`) directly, or via a future Settings UI. Acceptable for an internal app with 1–3 known users on the same network. **Revisit when the user count grows or someone forgets their password in practice.**

- **No account lockout / rate limiting on login.** Failed login attempts have no throttling — an attacker who knows the email could brute-force the password endlessly. Acceptable trade-off for an internal app behind a single login wall with a known small admin set. **Revisit if user count grows materially, if the app is ever exposed beyond the office network, or if we add public-facing surfaces.** Implementation options when needed: edge-rate-limiting via Vercel firewall, in-app cooldown table tracked in Postgres, or a `failedAttempts` + `lockedUntil` columns on `User`.

- **Customer Excel import not built.** Manual entry only in Phase 2.1; no bulk-load mechanism. **Revisit as a Phase 2.x polish** if onboarding a long list of regulars (e.g., migrating from a paper ledger) becomes needed. Implementation when needed: ExcelJS-based reader on a `/customers/import` route, with a preview-and-confirm step + dedup detection (see below).

- **Customer dedup detection not built.** No warning if a user creates two records with similar names (`"Rajeshbhai"` vs `"Rajesh Bhai"`, or with/without a trailing whitespace). Manual data-entry will produce dupes over time. **Revisit in Phase 3** when the Sale party-autocomplete makes the duplication user-visible (typing "Raj" surfaces both records and the user notices). Implementation when needed: levenshtein / unicode-NFKD-normalized name match + phone-prefix match, surfaced as "Did you mean…?" suggestions on the Add modal.

  **Phase 6 update — walk-in deduplication migration for historical data deferred.** Phase 6's auto-promotion closes the gap **forward**: every new walk-in saved with a phone is either linked to an existing master-data row or auto-creates one. **Pre-Phase-6 walk-ins are NOT retroactively promoted** — if any existed before the deploy, they remain `customerId IS NULL` rows with only the snapshot strings, even when their phone matches a Customer that exists today. At present production has **zero** historical walk-ins (audited during the Phase 6 build via `scripts/audit-phone-normalization.mjs` — `sales WHERE partyPhone IS NOT NULL` was 0; `purchases` was 0; no customer/supplier had non-digit chars in `phone`), so no backfill is required. If real usage later produces a meaningful pile of historical walk-ins, the migration shape is straightforward: scan `sales WHERE customerId IS NULL AND partyPhone IS NOT NULL`, group by `normalizePhone(partyPhone)`, create missing Customers, set `sales.customerId` to link existing rows. Mirror for Purchases. **Priority: low** unless dirty historical data accumulates faster than the user notices.

- **No UI to restore soft-deleted customers.** `softDeleteCustomer` sets `deletedAt`; the page query filters `deletedAt: null`; deleted rows preserve history but are invisible from the app. To restore manually: `UPDATE customers SET "deletedAt" = NULL WHERE id = '…'` via Supabase MCP. **Build a `Settings → Deleted records` page if this becomes painful** — likely after Phase 6 when other entities have the same soft-delete pattern and consolidating into one admin view is worthwhile.

- **Real-DB integration tests.** Current tests mock Prisma via `vitest-mock-extended`. End-to-end tests against a real test database (a separate Supabase project, or local Postgres via `prisma migrate deploy` against a sandbox) are deferred to Phase 8 polish. The mocked tests catch logic bugs (validation, transform behavior, action wiring, component interactions). Real-DB tests would additionally catch schema-vs-Prisma-client desync, query-optimizer surprises, and Supabase-specific behavior (e.g., pooler quirks, JSON column edge cases). Revisit when migration count grows enough that schema-client drift becomes a real risk, or when we hit a production bug that mocked tests would have missed.

- **Playwright / browser-driven tests.** No e2e or visual-regression tests this phase. Component tests under `jsdom` catch behavior but not CSS rendering, hover states, animations, layout, or cross-browser issues. Deferred to Phase 8 polish. Implementation when needed: `@playwright/test` with screenshot-diff against a baseline, run against a dev-server-on-CI build. Cost: ~150 MB Chromium download + a few hundred ms per test run.

- **Shared test fixtures across entities.** *[confirmed need after Phase 2.3]* `makeCustomer()`, `makeSupplier()`, and `makeEmployee()` row factories are now duplicated across six test files (one per entity × `actions.test.ts` + `<entity>-table.test.tsx`). All three share the same `id, name, phone, ..., createdAt, updatedAt, deletedAt` shape; Employee additionally has `type` + `monthlySalary`. The right extraction shape is now visible: a `buildPartyFixture(overrides)` in `src/test/fixtures/party.ts` for the Customer/Supplier shape, and `buildEmployeeFixture(overrides)` in `src/test/fixtures/employee.ts` for the Employee shape — keeping them as two functions (not one generic) preserves type-narrowness for callers. Also worth lifting: the `fakeSession = { user: { id, email, name, role } }` constant duplicated across all three `actions.test.ts` files, into a shared `src/test/fixtures/session.ts`. **Revisit early in Phase 3** before Sales adds the fourth entity (which would push the count to 8 duplicated factories).

- **Radix `<DialogDescription>` missing on every modal.** Customer, Supplier, Employee, and Sale form modals + detail modals (eight total now after Phase 3.1) all render `<DialogHeader><DialogTitle>…</DialogTitle></DialogHeader>` without a sibling `<DialogDescription>`. Radix logs a dev-mode `console.warn` per open: *"Missing `Description` or `aria-describedby={undefined}` for {DialogContent}."* Pure accessibility-pattern violation — no runtime impact, no failing tests, no a11y-tree breakage in practice — but the warning litters dev logs and the spec is what it is. Fix options when revisited: (a) add a one-line `<DialogDescription>` to each `<DialogHeader>` with a contextual sentence; (b) pass `aria-describedby={undefined}` on each `<DialogContent>` to explicitly opt out. **Revisit as Phase 2.4 polish or roll into the Phase 8 accessibility pass.**

- **Sale receipts (Phase 3.4).** Sale schema deliberately has NO `receiptUrl` column in Phase 3.1. Will be added when the Supabase Storage upload pipeline is built (signed-URL generation, image preview thumbnail, PDF passthrough, size/type validation). Schema migration when needed: single `receiptUrl: String?` column on Sale + a parallel one on SaleReturn (Phase 3.3). Bucket setup: a private `receipts` bucket with per-row access scoped by the authenticated user's session. **Revisit in Phase 3.4** after Phase 3.2 (payments) and Phase 3.3 (returns) are built.

- **Sale returns + `refund_due` status (Phase 3.3).** `SaleReturn` model not yet created. Like payments, the helper signature already accepts `returnTotal?: bigint`. Phase 3.3 will populate via `SUM(SaleReturn.total)` per Sale and pass it to `computeSaleStatus`. The `refund_due` branch fires when `(total − returnTotal) − paidAmount < 0` (overpaid or returns exceed payments). All four status branches are already covered by `sale-status.test.ts`. **Revisit in Phase 3.3** after payments.

- **Status chip "completed" uses blue, not green.** `SaleStatusChip` maps `completed → bg-secondary-container` (blue) because the design system has no green token. Functional but conventionally green = success across most UIs. Fix when revisited: extend `globals.css`'s `@theme` block with a `success` / `on-success` token pair following the existing `error` / `on-error` pattern, then update `SaleStatusChip` + the Employee `TypeChip` (and any future status chips) to use it. **Revisit during a design-system polish pass** — low priority since the visual distinction is still present (blue vs amber vs error red).

- **`timestamp without time zone` vs `timestamptz` discrepancy across all tables.** Verified via Supabase MCP during Phase 3.1 — `users`, `customers`, `suppliers`, `employees`, `sales`, `sale_payments`, and `sale_returns` all use `timestamp without time zone` for their date/timestamp columns (`createdAt`, `updatedAt`, `deletedAt`, `date`, etc.), but CLAUDE.md §4 / §6 claim "timestamptz UTC." This is Prisma's default mapping for `DateTime`. **Data is correct in practice** — Prisma's `@default(now())` returns a UTC value, `@updatedAt` sets UTC, and the client always reads back a JS `Date` (which JS treats as UTC internally regardless of column type). But the column type isn't *enforcing* UTC at the DB level — a direct-SQL `INSERT` with a local-time-without-timezone literal would store that local time uninterpreted. **Revisit during a database hardening pass** — migration to convert all timestamp columns to `timestamptz`. Not urgent: app code goes through Prisma which is consistent; ad-hoc SQL inserts via Supabase MCP need to be UTC-explicit either way. Discovered Phase 3.1 schema verification.

- **`PaymentPanel` duplication across Sales and Purchases.** Two parallel ~400-LOC files (`sales/payment-panel.tsx` + `purchases/payment-panel.tsx`) with behavioral differences that go beyond pure parameter substitution: label inversions ("Outstanding" ↔ "Owed to supplier", etc.), the REFUND-row color flip (`text-error` ↔ `text-secondary`), the minus-vs-plus prefix on refund amounts. **Largest single-file duplication in the codebase.** Do NOT extract without a third instance — the behavioral differences are subtle enough that parameterizing them adds props (`directionLabel: "in" | "out"`, `refundClassName: string`, ...) that the next reader has to learn before understanding what a single render does. **Revisit when Phase 6 (karigar payments) adds a third payment-like context.** If karigar payments are different enough (e.g., no return-driven `refund_due`, no overpayment guard with formatted error), `PaymentPanel` stays at two stable parallel instances — duplication is OK when the alternative is a prop-stuffed abstraction. Discovered Phase 4 build.

- **Goodwill refunds without a product return are NOT supported by UI.** The "+ Issue refund" button only appears when `sale.status === 'refund_due'`, which requires `paidAmount > effectiveTotal` — and `effectiveTotal < total` requires a return to exist. **Real business case**: customer is unhappy, partial price negotiation, goodwill credit owed without a physical return. **Current workaround**: record a `SaleReturn` with `qtyReturned > 0` and `refundAmount` covering the goodwill amount (slightly distorts the "items returned" data but lets the refund flow happen). **Direct support** would require either (a) decoupling the refund button from `refund_due` status (always show "+ Issue refund" alongside "+ Record payment" in some UI affordance — e.g., a dropdown or split button), or (b) allowing `SaleReturn` rows with `qtyReturned === 0` (changing the schema's `positive` validation to `nonnegative` and accepting that "refund without return" is a legal category of return event). **Revisit if encountered in real usage.** Decoupling (a) is cleaner long-term; (b) is the lighter-touch fix. Discovered Phase 3.3 planning.

- **No staff (non-admin) user yet.** Phase 4.5 plan included creating a STAFF-role account for the accountant; deferred at the user's request pending the accountant's details. Single ADMIN account in prod for now. **Revisit when the accountant is ready to log in** — direct SQL insert against `users` table (see `docs/HANDOFF.md` § 7) until the user-management UI exists.

- **No user-management UI (create / change-password / forgot-password).** Production currently requires direct SQL or env-var-driven `prisma db seed` to create or modify users. Acceptable for 1–3 known users on a small internal app, but every operation that should be a button click is currently a DB write. **Priority increased after Phase 5** — with four roles in production (1 ADMIN + 3 test accounts), the manual-insert path is more brittle (role typos, missed fields, no validation that `passwordHash` is bcrypt-shaped). A small phase (~3–4h) before adding any 5th user is worth doing. Scope: invite-by-email flow, role-selection dropdown (typed against the four-role enum), password reset trigger, current-password change for the logged-in user. **Revisit before adding a 5th user, or when an admin needs to onboard staff without DB access.** Ties together with the existing "Invite user flow not built" / "No password reset flow" / "Admin password rotation deferred" items — solving this one resolves the trio.

- **Casting and Plating pages not yet built.** Routes `/casting` and `/plating` are gated by the proxy (only `ADMIN` and `CASTING_PLATING_MGMT` can reach them; everyone else redirects to `/dashboard`) and the sidebar shows them as "Soon" badges for those two roles. `CASTING_PLATING_MGMT` users currently see only their dashboard placeholder card. **Revisit in Phase 9** (casting + plating tracking, ~5–6h estimate). Phase 9 depends on Phase 8 (bill upload infrastructure). When the pages land, also drop the `enabled: false` flags on the two sidebar items in `src/app/(app)/sidebar.tsx` and remove the dashboard placeholder.

- **No staging / preview environment.** Prod is fed directly from `main`. There is no `staging` branch with a preview URL, no PR-preview deployments, no canary releases. Every push to `main` becomes production within ~60s. **Acceptable for current single-developer mode**; revisit if a second developer joins or if the change rate produces enough regressions to justify a gate. Vercel's Rolling Releases feature is the natural extension when needed.

- **No custom domain.** App is served from `jewlerytracker-darshan-somaiyas-projects.vercel.app`. **Revisit when ready to bind something like `app.shreecreation.com`.** Custom domains also bypass Vercel SSO Protection by default, so binding a domain is the cleanest path if you ever want to re-enable SSO for the `*.vercel.app` URL without locking yourself out. (SSO is currently OFF for this project since the app has its own Auth.js.)

- **No app-side backup automation.** Supabase Free tier provides daily backups retained 7 days. No custom `pg_dump` schedule, no off-cloud copy, no point-in-time recovery (a paid Supabase tier feature). For ~100 transactions/month this is probably fine — but if a destructive bug ships and isn't noticed for >7 days, recovery is impossible. **Revisit if data volume or operational risk grows.** Implementation when needed: a GitHub Action that runs `pg_dump` against `DIRECT_URL` weekly and uploads to a private bucket / repo asset.

- **No third-party observability** (Sentry, Datadog, Logtail, etc.). The only custom logging is the `console.error` in `src/lib/auth.ts`'s authorize wrapper. Vercel's built-in runtime logs are available but ephemeral (no long-term retention on free tier) and not queryable beyond keyword filter. **Revisit when a real prod error needs deep investigation** and the Vercel log retention proves insufficient. Sentry's Vercel integration is one-click for app errors; for DB / Prisma instrumentation Datadog's Vercel marketplace integration is the typical pick.

- **No CI / pre-deploy quality gate.** A push to `main` deploys without first running `npm run test:run` or `npx tsc --noEmit` in CI. A test-failing or type-broken commit will deploy if it survives the Vercel build (which only catches TS errors that block compilation, not Vitest failures). **Revisit when broken-main incidents start happening** — a GitHub Actions workflow that runs tests + typecheck on every push and blocks the Vercel deploy on failure is the standard fix. Cost: ~3 minutes per push, requires adding a CI config and possibly switching to a Vercel "deploy only on CI green" mode. Not urgent at single-developer velocity.

- **The Vercel CLI is not installed locally.** Convenience commands like `vercel logs --follow`, `vercel env pull`, `vercel inspect`, and direct `vercel deploy` from the CLI are unavailable. The Vercel Plugin / MCP tooling fills most of the gap (deployment trigger, runtime logs, build logs). **Install when something the MCP doesn't surface becomes painful** — `npm i -g vercel` is a single command.

- **Hydration warnings on date / datetime fields.** Production navigation logs ~9 instances of React error #418 ("Text content does not match server-rendered HTML") per session. Surfaced during the Phase 6 walkthrough (`scripts/walkthrough-p6-auto-promo.mjs` Browser diagnostics block). Almost certainly a date/timezone formatting mismatch between the server container (UTC) and the client browser (`Asia/Kolkata`) — `formatDate` / `formatDateTime` in `src/lib/format.ts` run on both sides and produce slightly different strings when the system clock crosses a date boundary in IST vs UTC, or when the locale defaults differ between Node and the browser. Doesn't affect functionality (all walkthrough assertions passed), but pollutes the browser console and indicates SSR drift. Diagnostic plan: enable React's non-minified runtime in a preview deploy → identify the offending render → pin timezone explicitly in the formatter on both sides (e.g., `Intl.DateTimeFormat('en-IN', { timeZone: 'Asia/Kolkata', ... })` for the server, matching the client). **Estimated effort: ~30 minutes** once the offending render is identified. Defer to a polish phase. Discovered Phase 6 walkthrough.

- **R2 orphan cleanup job (Phase 8 deferred).** `softDeleteBill` deletes the R2 object FIRST then tombstones the DB row; on R2-delete failure it logs the error and proceeds with the DB tombstone anyway, leaving the R2 object in the bucket without a referencing row. `confirmUpload` similarly leaves a FAILED row pointing at a possibly-stored R2 object if R2 cleanup mid-action fails. **At our scale** (~100 transactions/month × few bills each × tiny failure rate × 10MB cap) this is bucket bloat measured in single-digit MB/year. **Revisit when**: bucket size makes the storage cost noticeable, OR Phase 3.4 retrofit lands and bills start being created at production volume. Implementation: a weekly Vercel Cron that lists all R2 keys under `bills/`, joins against `bills.r2Key WHERE deletedAt IS NULL AND status='READY'`, deletes the unreferenced keys. Track in `KNOWN_GAPS.md` resolved section when shipped.

- ~~**R2 token currently scoped Admin Read & Write — should revert to Object R&W (Phase 8 follow-up).**~~ *(resolved 2026-05-19 — see resolved section below)*

- **Phase 3.4 receipt retrofit not yet wired (Phase 8 deferred).** Phase 8 ships the Bill table + R2 upload infrastructure + admin-only test page. Bills are NOT yet attached to Sales, Purchases, payments, casting entries, or plating entries — those entities have no UI affordance to upload a bill, and their detail modals don't render attached bills. **Revisit in Phase 3.4** (the original placeholder for "Sale receipts" — now broadened to cover Purchases + payments). When the retrofit lands, each Sale/Purchase form modal grows a file picker that calls `prepareUpload` with the appropriate `attachedToType` + `attachedToId`; detail modals render a list of attached bills with View / Delete actions backed by the existing `getBillViewUrl` + `softDeleteBill` actions. The role matrix in `src/lib/bill-access.ts` already covers the four planned types. Estimated effort: ~4-6h per parent entity (Sales / Purchases / casting / plating); Phase 9 (casting + plating) depends on this. Earlier "Sale receipts (Phase 3.4)" deferred entry above is the same item — keeping for context.

  **Update Phase 10 — partial resolution for Sales.** Bills are now attachable to Sales via the row-level `BillActionModal` (the inline `📎` action button). `SALE` was added to `ATTACHED_TO_TYPES` + the role write/read matrices. Sales/Purchases use the discriminator-only lookup (`getBillForEntity(attachedToType, attachedToId)` in `bills/actions.ts`) rather than a `billId @unique` FK on the entity row — they don't need the schema-enforced 1:1, and the discriminator covers payment-attached bills cleanly when those land. Purchases still don't have the row-level Bill modal wired (still on the legacy detail-modal-embedded path); Phase 10.5 handles that.

- **Phase 10.5 — Mirror UX to Purchases** — *[resolved on 2026-05-17]*. Purchases now has full form-as-page (`/purchases/new`, `/purchases/[id]/edit`) + read-only `PurchaseDetailModal` + inline action buttons (Add payment / Manage bill / Record return) in the row Actions column + prop-injection dispatch via `onSave` / `onAttach` / `onDetach` props on the shared action modals. Mirror executed via `scripts/_mirror-sales-to-purchases.mjs` (gitignored). One regression caught at walkthrough — `entityType="sale"` string literals leaked through the mirror, attaching 4 orphan SALE-typed bills to purchase IDs; fixed in commit `4686548` + production cleanup via direct SQL. Decision-lineage entry above documents the defences for future mirrors. **Casting + Plating mirror deferred to Phase 10.6** (now its own item below). **Next audit cycle: prune.**

- **Bill section inside the form page itself** — *[resolved on 2026-05-17]*. All four transactional form pages now include an inline pre-upload bill section between line items and totals: file picker → `BillPreview` → on submit, `create*/update* → prepareUpload → R2 PUT → confirmUpload → navigate`. Casting/Plating add a final `attach*BillTo*Entry` step after `confirmUpload` (FK-based). Sales/Purchases skip that step (discriminator-only). Edit mode adds NEW bills only; replace/remove uses the row-level 📎 modal. On upload failure post-entry-create, the entry stays saved and a banner directs the user to the row-level modal for retry. Sales completed Phase 10.5; Purchases/Casting/Plating completed Phase 10.6. Decision-lineage entry above documents the error-semantics choice. **Next audit cycle: prune.**

- **Phase 11.1 — Mobile foundation + transactional UX** — *[resolved on 2026-05-18]*. Mobile-responsive layouts for Sales/Purchases/Casting/Plating + the shared action modals + the sidebar drawer. New shared infrastructure: `Sheet` primitive (`src/components/ui/sheet.tsx`), `ResponsiveDialog` wrapper (`src/components/responsive-dialog.tsx` — renders Dialog on md+, full-width bottom Sheet below md), `ResponsiveTable` wrapper + `MobileCard*` primitives (`src/components/responsive-table.tsx`), `useIsMobile` hook (`src/lib/use-is-mobile.ts`, SSR-safe default = desktop). Sidebar now hamburger-triggered Sheet drawer on mobile + always-visible aside on desktop; layout adds a fixed mobile header bar with hamburger + wordmark + role chip. The 4 transactional tables wrap their existing TanStack tables in `<ResponsiveTable>` with entity-specific mobile cards (Sales/Purchases: Pay/Bill/Return; Casting/Plating: Pay/Bill — Phase 9 decision). The 3 action modals + 4 transactional detail modals swap Dialog → ResponsiveDialog. Touch targets ≥ 44×44px on all mobile action buttons via `min-h-[44px]`. Viewport meta tag added to `src/app/layout.tsx`. All 990 existing tests pass via a `window.matchMedia` stub in `vitest.setup.ts`; `npx tsc --noEmit` clean. Dashboard grid verified responsive-out-of-the-box (already `grid-cols-1 md:grid-cols-N`). **Deferred to Phase 11.2** (see below): master-data tables/modals, form-page mobile layout, mobile walkthrough, ~70 mobile-branch tests, CLAUDE.md §2/§6 + TESTING.md updates. **Next audit cycle: prune.**

- **Phase 10.6 — Mirror UX to Casting + Plating** — *[resolved on 2026-05-17]*. Casting + Plating now have form-as-page (`/casting/new`, `/casting/[id]/edit`, `/plating/new`, `/plating/[id]/edit`) + read-only `CastingDetailModal` / `PlatingDetailModal` + inline action buttons (Pay, Bill — no Return; Phase 9 decision: outsourced services have no returnable-goods workflow) + prop-injection dispatch via the shared `PaymentActionModal` and `BillActionModal`. Bill flow uses FK-based attachment: the entry's `billId @unique` FK is set via `attachBillToCastingEntry` / `attachBillToPlatingEntry` after `confirmUpload`, cleared via `detachBillFrom*Entry` before replace soft-deletes the old bill. Mirror executed via new `scripts/_mirror-sales-to-casting.mjs` (Sales → Casting) + existing Phase 9 `scripts/_mirror-casting-to-plating.mjs` (Casting → Plating); both gitignored. One leak caught at the casting↔plating mirror — `castingPlatingVendor` (camelCase Prisma property) was missing from `PRESERVE_PAIRS` despite the Phase 9 onboarding note saying it should be there; fixed the script + re-ran (see decision-lineage entry above for the doc-vs-impl drift pattern). 17/17 walkthrough checks pass (14 plan + 3 sub-checks); 72 net new tests bring totals to 990 (was 918). All four transactional entities now uniform on the new UX pattern. **Phase 11 (mobile)** is the natural next step — building responsive layouts on a single consistent pattern is meaningfully simpler than four. **Next audit cycle: prune.**

- **Aria-describedby warnings on Radix DialogContent (Phase 10.5 noise).** Pre-existing across all dialog-using tests — every `<DialogContent>` without a sibling `<DialogDescription>` logs `Warning: Missing 'Description' or 'aria-describedby={undefined}' for {DialogContent}`. Fires both in dev console and in vitest stderr. Optional cleanup paths: (a) add a one-line `<DialogDescription>` to each `<DialogHeader>` with contextual copy, or (b) pass `aria-describedby={undefined}` on each `<DialogContent>` to explicitly opt out. Same item as the older Phase 2.4 entry above; tracking here too because Phase 10.5 added 3 new action modal tests that surface the warning each. **Revisit during the Phase 8 accessibility polish pass.**

- **Periodic orphan-bill audit (Phase 10 follow-up).** The bill-replace flow soft-deletes the old `Bill` row then uploads the new one. If a failure occurs between `softDeleteBill` and `confirmUpload`, the entity ends up with no attached bill — but a PENDING bill row may linger if `prepareUpload` succeeded then R2 PUT or `confirmUpload` failed. Audit query: `SELECT count(*) FROM bills WHERE status = 'PENDING' AND "uploadedAt" < NOW() - INTERVAL '1 hour';` — should be 0 in steady state. Non-zero indicates partial failure. **Manual audit for now**; automate as a Vercel Cron when usage grows or after the first incident. Pairs with the existing Phase 8 "R2 orphan cleanup job" deferred item — both about reconciling DB ↔ R2 drift, both deferred to the same future automation pass.

- **PartyPicker prefill at unit-test scale (Phase 10 testing limitation).** The PartyPicker's controlled-input pattern (RHF `value` driven through `watch` → component prop → input value attribute) doesn't flow cleanly under jsdom's strict-mode double-render — `#party-name-input` shows up empty in tests even when the form's underlying state has the prefilled value. Prefill IS correct in production (verified at integration scale by Phase 10 walkthrough Step 5). The unit test in `sale-form.test.tsx` scopes its prefill assertion to line-item fields (which prefill cleanly via RHF `reset()`). **Workaround documented in the test comments**; not blocking. **Revisit** if anyone refactors PartyPicker — a switch to RHF's `Controller` wrapper might fix the unit-test flow.

- **Phase 11.2 — Complete mobile migration (master data + forms + walkthrough + tests + docs)** — *[resolved on 2026-05-18]*. Mobile coverage now codebase-wide. **Form pages (8)**: `sales|purchases|casting|plating × new|[id]/edit/page.tsx` get `p-4 md:p-10` + `text-2xl md:text-3xl` + responsive header spacing. **Form components (4)**: `sale-form.tsx` / `purchase-form.tsx` / `casting-form.tsx` / `plating-form.tsx` use the **`md:contents` trick** for line items — outer `grid-cols-1 md:grid-cols-[…]` with inner `grid-cols-[1fr_1fr_44px] md:contents` sub-group for qty/rate/× (or weight/rate/× on casting/plating), desktop-only line-total via `hidden md:flex` inside the sub-group, mobile-only line-total row 3 via `md:hidden` outside; form footer `sticky bottom-0 -mx-4 px-4 pb-4 bg-surface md:static md:flex-row md:justify-end`; remove button `h-11 w-11` (44×44 touch target on mobile, `h-8 w-8` on desktop). **SaveDropdown** rewired to `flex w-full md:inline-flex md:w-auto`; primary `flex-1 md:flex-none h-11 md:h-10`; chevron `shrink-0 h-11 md:h-10`. **Master tables (4)**: `customers|suppliers|employees|vendors-table.tsx` wrap their TanStack tables in `<ResponsiveTable>` with simpler mobile cards (name + phone + entity-specific aggregates — no inline action buttons; mutations go through detail modal Edit). **Master list pages (4)**: `customers|suppliers|employees|vendors/page.tsx` get `p-4 md:p-10` + responsive header. **Detail modals (4) + Form modals (4)** for master data: `Dialog → ResponsiveDialog` swap; footer `flex-col-reverse md:flex-row` with all buttons `h-11 md:h-9` or `h-11 md:h-10`. **Tests**: +67 (990 → 1057) — mobile-viewport blocks added to customers/employees/sales-form/purchase-form/casting-form/plating-form/save-dropdown; new files for suppliers + vendors mobile-card coverage; `src/test-utils/viewport.ts` helper for `mockMobileViewport()` / `mockDesktopViewport()`. **TypeScript** clean. Phase 11.1 hotfix patterns followed verbatim (no semantic divergence). The mobile walkthrough is product-owner verified at DevTools 390x844 (matchMedia unit tests verify branch logic but JSDOM doesn't evaluate CSS media queries — viewport-level visual regression detection requires real-phone or DevTools). **Phase 11.5** (visual restyle to light theme) is the natural next step. **Next audit cycle: prune.**

- **Party-picker DOM ID convention inconsistency (Phase 10.6).** Sales and Purchases use a shared, entity-agnostic `#party-name-input` / `#party-phone-input` id on the PartyPicker's input fields. Casting and Plating use entity-prefixed ids — `#casting-party-name` / `#casting-party-phone` and `#plating-party-name` / `#plating-party-phone`. Discovered when the Phase 10.6 walkthrough's first run failed at Step 2 with a 30-second locator timeout — the script assumed all four entities used the Sales/Purchases convention. Fixed the walkthrough script's 4 selector references. **Minor inconvenience**: any future cross-entity walkthrough or e2e script has to derive ids per entity. **Polish-phase cleanup**: standardize on one convention (recommend `#${entity}-party-name` for consistency, since the prefixed form is more self-documenting in the DOM tree and avoids the cross-page collision risk of a generic `party-name-input`). Migration is a single rename in each party-picker.tsx file + matching test selector update; ~5 lines per entity, low risk. **Not blocking**: production works correctly with either convention, and the walkthrough script now compensates.

## Onboarding notes for future contributors

Things that aren't gaps but trip up new sessions.

- **MCP authentication is per-developer.** `.mcp.json` at project root declares the Supabase MCP server, but OAuth tokens are stored in each developer's local Claude Code state — not in the repo. A new session in this folder must run `claude /mcp` in a regular terminal (outside the IDE), select `supabase`, and complete OAuth before MCP tools work. The `.mcp.json` file itself is committed; tokens are not.

- **Dev server default port may be 3001, not 3000.** If another long-running node process holds port 3000, `next dev` falls back to 3001 with a `⚠ Port 3000 is in use` warning. Check the dev.log / terminal output for the actual port before opening the browser. To free port 3000: `Get-Process node` (Windows) or `lsof -i :3000` (mac/linux) → identify the holder → kill it if it's a stale dev server.

- **Tailwind v4 — theme tokens live in `src/app/globals.css`, NOT in a JS config file.** Don't go looking for `tailwind.config.ts` — it doesn't exist. All color/font/radius tokens are defined in the `@theme { … }` block inside `globals.css`. If you need to add a new color or change a token, edit `globals.css`. The `@theme` directive auto-generates Tailwind utility classes (`bg-surface-container`, `text-on-primary`, etc.) and CSS custom properties (`var(--color-surface)`) from the declarations.

- **Supabase keys use the new `sb_publishable_…` / `sb_secret_…` format**, not legacy JWTs. The env var names in `.env.local` retain the legacy `NEXT_PUBLIC_SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY` convention from the original spec, but the values are new-format. `@supabase/supabase-js@2.105+` accepts both formats. Don't be surprised that the values don't look like JWTs.

- **DB password contains URL sub-delim characters** (`,` and `*`). It's URL-encoded as `%2C` and `%2A` inside both `DATABASE_URL` and `DIRECT_URL` so Prisma's URL parser doesn't choke. If you regenerate the password and it contains other reserved characters (`+`, `&`, `?`, `#`, `/`, `@`, `:`, space), URL-encode them too. The raw password roundtrips correctly server-side.

- **Currency is stored as integer paise**, not rupees-with-decimals. `₹1,234.56` ↔ `123456` paise. Never store, sum, or arithmetic-on currency as a float anywhere. **Three-layer pipeline** established in Phase 2.3 (`Employee.monthlySalary` is the first monetary column; Sales / Purchases / FixedSalary / WorkEntry follow the same pattern in Phase 3+):
  1. **Prisma column:** `BigInt? @map("monthly_salary")` — paise integer math, safe for any salary range.
  2. **Schema:** validates rupees as `z.number().int().positive().nullish()` — wire-format identical on client send and server re-parse. **Do NOT** put a `.transform(v => BigInt(...))` in the schema — see KNOWN_GAPS decision lineage for the "wire-format mismatch" reason.
  3. **Action:** a local `toPrismaData()` helper converts rupees → `BigInt(Math.round(value * 100))` paise at the Prisma write only. On return, `serializeEntity()` (in `<entity>/types.ts`) converts BigInt back to `number` for JSON safety. UI receives `EntityForClient` with `monthlyXxx: number | null` and renders via `formatCurrency(paise)` from `src/lib/format.ts`.

- **Adding `jobType` to `PieceEntry` later (Phase 18 follow-up).** The Phase 18 piece-entry model is intentionally flat — one row per `(employee, day)` with no job-type discrimination. If the workshop ever runs multiple piece-rate jobs concurrently for the same karigar (e.g. ear-stud vs necklace at different rates), the extension shape is straightforward: (1) migration adds `jobType String?` to `piece_entries` (nullable, no default — existing rows stay NULL meaning "unclassified"); (2) bulk-piece-entry-section.tsx extends the row shape with a `jobType` picker (sticky-default at form level or per-row); (3) `Employee.ratePerPiece` becomes the default for the "primary" job; secondary jobs use the picker's `(jobType, ratePerPiece)` selection; (4) `getOutstandingWages` doesn't need to change because wage aggregation is still per-employee — but reporting / per-job-type rollups become a separate read path. **Don't add `jobType` proactively** — it's purely additive when the need arises, and starting with a discriminator that has only one value forces a friction-adding UX decision. The Phase 18 flat model is the right starting point even if jobType eventually lands.

- **Server/client module-boundary discipline.** Three classes of bug have surfaced from incidental imports crossing the boundary; all share the same lesson — *what's reachable from a client component must be bundleable for the browser*. Before adding a new entity or import, ask "what does this pull along with it?"
  - **Zod schemas exported from `'use server'` files** are compiled into client-reference stubs by Next.js (Phase 2.1). Keep schemas in `schema.ts` (no directive); actions import the schema.
  - **Type-only exports from `'use server'` files** also break — Next.js's actions loader produces `ReferenceError: TypeName is not defined` at runtime when a client component imports the type (Phase 2.3). Keep client-facing types and serializer helpers in `types.ts`, not in `actions.ts`.
  - **Prisma enum value imports** (e.g. `import { EmployeeType } from "@/generated/prisma"`) pull `@prisma/client/runtime/client.mjs` (which uses `node:module`) into the client bundle (Phase 2.3). Use `import type { EmployeeType }` for type-only references; use `z.enum(["FIXED", "LABOUR"])` with literal strings for runtime validation. Never re-export Prisma's runtime enum values to code that crosses the boundary.

  General principle: if a value (not a type) crosses from server-only territory to client-reachable territory, audit its transitive imports for Node-only modules (`node:*`, `pg`, `fs`, `crypto` outside Web Crypto), Next.js server-action stubs, or anything carrying `'use server'`.

- **Status is derived, not stored.** Don't add a `status` column to `Sale` or `Purchase`. See `CLAUDE.md` §5 for the full rationale and the TypeScript string-literal union pattern.

- **Schema change workflow.** When adding or modifying a Prisma model:
  1. Edit `prisma/schema.prisma`
  2. `npx prisma migrate dev --name <description>` — applies the migration to the DB via `DIRECT_URL`
  3. `npm run prisma:generate` — Prisma 7 doesn't auto-regenerate after `migrate dev`; this also writes the `src/generated/prisma/index.ts` barrel
  4. **Restart the dev server** — `src/lib/prisma.ts` caches the `PrismaClient` instance in `globalThis.prisma` to prevent connection exhaustion under HMR. New models will be `undefined` on the running singleton until the process restarts (`prisma.customer` would throw `Cannot read properties of undefined (reading 'findMany')`). Discovered Phase 2.1 Task 7.

  Fresh clones get steps 3 + 1-via-`migrate deploy` from the `postinstall` hook in `package.json` — but during active development, all four steps are manual.

- **Build-cache cousins — `rm -rf .next/` is the universal first-aid kit when "I changed something and the dev server is still serving stale behavior."** Turbopack's compiled chunks under `.next/` can pin to a build's view of env-dependent module evaluation, CSS token resolution, or generated-client shape. Symptoms seen across phases:
  - **Phase 1.6 Task 5:** Tailwind CSS chunk served stale `--spacing-md` mapping after `globals.css` edits; HMR didn't pick up the change. Fix: `.next/` nuke + cold restart.
  - **Phase 2.1 Task 7:** Prisma client singleton (`globalForPrisma.prisma`) was instantiated before a new model was added; even after dev-server restart, the new model was `undefined`. Fix: `.next/` nuke + cold restart.
  - **Phase 2.1 credential rotation Task B:** Prisma reported `Authentication failed against the database server` after a verified-correct DB password change. Direct `pg` driver test with the same `DATABASE_URL` succeeded; only the dev server's Prisma adapter failed. Fix: `.next/` nuke + cold restart.

  **First diagnostic step** when a change to env vars, `globals.css`, `schema.prisma`, or any generated code doesn't take effect after a normal dev-server restart: stop the dev process, `rm -rf .next/`, restart. Saves hours over speculative debugging. The cost is a one-time recompile (~5 sec).

- **Postgres `anon` and `authenticated` roles have no privileges on the `public` schema.** New tables don't need RLS policies for security (the only roles that can access them are `postgres` / `service_role`, both of which `BYPASSRLS`). Adding RLS as defense-in-depth is fine. If you see "RLS disabled" advisories from Supabase, **the threat is mitigated by the grant revocation** — see decision lineage. Schema-level USAGE remains for the two roles (inherited from the `PUBLIC` role) but is harmless without table grants.

- **Prisma client output lives at `src/generated/prisma/` (gitignored).** The directory is regenerated by `npm run prisma:generate`. Never edit files inside it directly. Importing pattern: `import { prisma } from '@/lib/prisma'` for the singleton; `import { PrismaClient, Prisma, $Enums } from '@/generated/prisma'` for type imports.

- **First-time setup after `git clone`.** Required steps before the dev server is useful: (1) `npm install` (runs `postinstall` → `prisma generate` → barrel write automatically); (2) populate `.env.local` from `.env.example` — values come from Supabase dashboard + a fresh `openssl rand -base64 32` for `AUTH_SECRET` + a chosen `SEED_ADMIN_EMAIL` / `SEED_ADMIN_PASSWORD` (12+ chars); (3) `npx prisma migrate deploy` to apply existing migrations to your DB; (4) `npx prisma db seed` to create the admin user; (5) `npm run dev` to start the server; (6) sign in at `/auth/login` with the seeded credentials. Skipping step 4 means there's no user to log in as.

- **JWT sessions — no DB session table.** Auth.js is configured with `session: { strategy: 'jwt', maxAge: 30 days }`. Sessions are stateless JWE cookies signed/encrypted with `AUTH_SECRET`. There is no `sessions` table in the DB and no `@auth/prisma-adapter` involvement (despite the package being installed — it's available for a future switch but not wired up). To invalidate all sessions globally (e.g. after a security incident), **rotate `AUTH_SECRET` in `.env.local`**. Every existing cookie becomes undecryptable; everyone is forced to re-authenticate.

- **Heading fonts cascade from `@layer base`, not from per-element classes.** All `h1`–`h6` elements automatically use Geist via `html, body { font-family: var(--font-sans); }` + `h1, h2, h3, h4, h5, h6 { font-family: var(--font-display); }` in `src/app/globals.css`. **Don't add `font-display` to heading elements** — it's redundant and creates inconsistent patterns across pages. Only use `font-display` on **non-heading** elements that should look like headings (e.g. the dashboard card's stat number uses `font-display text-2xl font-semibold` on a `<p>` because `<p>` defaults to Inter).

- **Tables with Prisma `@updatedAt` — direct-SQL `UPDATE`s via Supabase MCP must set `"updatedAt" = NOW()` explicitly.** Prisma's `@updatedAt` is application-managed (the Prisma client sets the column on every update call), NOT a DB trigger. All app code goes through Prisma and inherits this automatically — but ad-hoc `UPDATE` statements run through the Supabase MCP `execute_sql` tool (for debugging, manual restores, role flips, etc.) won't touch `updatedAt` unless you include it: `UPDATE users SET role = 'STAFF', "updatedAt" = NOW() WHERE id = '…'`. The column has a NOT NULL constraint, so omitting it on an `UPDATE` will be silent unless you've cleared it, in which case the statement fails.

- **Per-entity folder convention.** Each CRUD entity (Customers built in Phase 2.1; Suppliers, Employees, Sales, Purchases follow) lives under `src/app/(app)/<entity>/` with this fixed layout:
  - `page.tsx` — server component, queries the entity list, embeds the client table
  - `schema.ts` — zod validation, **plain TS, no `'use server'` directive** (see decision lineage for why)
  - `actions.ts` — `'use server'`, imports the schema, exports `create<Entity>` / `update<Entity>` / `softDelete<Entity>`
  - `<entity>-table.tsx` — client component, TanStack Table v8, search + sort + zebra striping + Edit/Delete row actions
  - `<entity>-form-modal.tsx` — client component, RHF + zodResolver with the triple-generic pattern; one component serves both add and edit (mode toggled by `customer` prop)
  - `<entity>-detail-modal.tsx` — client component, read-only view with Edit/Delete footer (Delete shows inline confirmation, no second-modal layer)

  Soft delete is mandatory: every entity that touches business data gets a nullable `deletedAt` column + an index on it. List queries filter `where: { deletedAt: null }`. Restore is via direct DB UPDATE (see deferred items).

- **Running tests.** Three scripts:
  - `npm test` — Vitest watch mode for development (re-runs affected tests on save)
  - `npm run test:run` — one-shot run, used by CI / pre-commit hooks
  - `npm run test:coverage` — one-shot with `@vitest/coverage-v8` baseline (Phase 2.2.5 baseline: ~71% statements project-wide, ~66% for the customers folder, ~87% for `src/components/`; will improve as new test categories land)

  Run a single test file: `npx vitest run path/to/file.test.ts`. Run a single test by name: `npx vitest run -t "rejects empty name"`.

- **Adding a new entity (Phase 2.3 onward).** The per-entity scaffolding convention above now extends to test coverage. Each entity must include: `schema.test.ts` (zod-only, no mocks), `actions.test.ts` (mocked Prisma + auth-guards + next/cache), and a component test on the entity's table (`<entity>-table.test.tsx`). Use the Customer/Supplier files as templates (~24 schema + 11 action + 14 component tests per entity). **Don't extract shared test helpers preemptively** — extraction belongs in a follow-up phase after three entities exist and the right shape is visible.

- **Test count reporting from Phase 2.3 onward.** The "Test count delta" line in every phase report stops being `N/A`. Format: `<previous total> → <new total> (+N net new, M skipped with reason)`. Skipped tests must include a reason (e.g., "skipped: covered by Phase 2.2.5 component test on identical surface"). `N/A` is no longer an acceptable value once tests exist for the layer being modified.

- **Test inputs to actions match `z.output<schema>`, not the raw form input shape.** Unit tests bypass the form layer and call the server action directly. The action's exported parameter type is `z.infer<typeof schema>` which equals `z.output` — the post-coercion shape. So test fixtures must construct values matching the OUTPUT, not the pre-coercion FORM shape. Concrete example from Phase 3.1: `SaleInput.date` is `Date` after `z.coerce.date()`, so `validInput()` must construct `new Date("2026-05-14T00:00:00Z")`, not the form's `"2026-05-14"` string. The schema's coercion path is exercised separately in `schema.test.ts`. **TypeScript catches this at `tsc --noEmit` time**; if you see "Argument of type 'string' is not assignable to parameter of type 'Date'" pointing at a test helper, the fix is the helper, not a cast. (Currency is the inverted case but works the same way: `rate: number` rupees stays a number in the schema output, and the action does the BigInt conversion internally — tests pass rupee numbers, not paise BigInts.)

- **`fireEvent.focus()` for jsdom React onFocus handlers.** Programmatic `element.focus()` in jsdom dispatches a native focus event, but it does NOT reliably trigger React's synthetic `onFocus`/`onBlur`/`onMouseEnter`/etc. handlers. Tests that rely on these handlers firing (e.g., a dropdown that opens on input focus) need explicit React-event dispatch: `import { fireEvent } from '@testing-library/react'` then `fireEvent.focus(input)`. `userEvent.click(input)` also works for the focus case (it dispatches the full mousedown→focus→mouseup→click sequence through React). Discovered Phase 3.1 — five party-picker tests failed because `input.focus()` didn't open the dropdown. Same gotcha applies to any synthetic-only React event handler.

- **Live-updating modal pattern — detail modals that show mutable data must derive, not snapshot.** When a detail modal exposes inner UI that mutates the entity (Phase 3.2 payments; Phase 3.3 returns; Phase 3.4 receipts; future status-changing actions), do NOT hold the entity as `useState<EntityForClient>(viewing)`. After `router.refresh()` the parent re-fetches but the modal's local state still references the *stale* pre-mutation object → status chip stays "Pending", payment list doesn't update, outstanding balance doesn't recompute. **Canonical pattern**: `useState<string | null>(viewingId)` + derive via `const viewing = viewingId ? parentDataArray.find(x => x.id === viewingId) ?? null : null`. After mutations call `router.refresh()`, the parent re-fetches → the derived value resolves to the updated row → modal re-renders with fresh data, no close-and-reopen needed. See `src/app/(app)/sales/sales-table.tsx` for the canonical implementation (`viewingSaleId` + the `sales.find` lookup). **Use this pattern by default** for any future detail-modal pattern where inner UI can mutate the underlying entity. Discovered Phase 3.2.

- **Unicode minus-sign regex gotcha.** When asserting on text that includes a minus sign (e.g., negative amounts in refund history like `−₹400.00`), the naive character class `[−-]` is an **invalid regex range** because `−` (U+2212 MINUS SIGN) comes AFTER `-` (U+002D HYPHEN-MINUS) in codepoint order. JS rejects with `SyntaxError: Invalid regular expression: ... Range out of order in character class`. **Fix**: put the literal `-` first (a leading `-` in a character class is always literal, never a range start): `[-–—−]` matches hyphen-minus, en-dash, em-dash, and math-minus. **Alternative**: explicit Unicode escapes `[-–—−]`. Encountered twice during Phase 3.3 — the Playwright walkthrough script `scripts/walkthrough-returns.mjs` failed with this exact syntax error, then the same trap re-surfaced in `payment-panel.test.tsx`'s assertion on REFUND-row rendering. `formatCurrency()` prepends U+2212 (not the ASCII `-`) via the panel's `amountDisplay` builder, so any test that asserts on negative-amount strings must use the literal-first character class. Onboarding rule: **if you write `[X-Y]` in a character class where X or Y is a Unicode codepoint above U+00FF, double-check the codepoint order and use the leading-`-`-as-literal trick instead.**

- **Label-inversion tests need both present-and-absent assertions.** When testing UI changes that flip semantics — Sales↔Purchases label inversions ("Outstanding" ↔ "Owed to supplier"), REFUND-direction color flips (`text-error` ↔ `text-secondary`), or any future semantic inversion — assert **both** that the new label IS rendered AND that the old/inverted label is NOT. Without the negative assertion, a copy-paste regression that leaves the original Sales-direction label still rendering would silently pass: the test author wrote `getByText(/^Owed to supplier:/i)`, the new label IS present, and the leaked old label sits invisibly alongside it. The dual assertion is what catches incomplete clones at the test layer. Example from `purchases/payment-panel.test.tsx`: `expect(screen.getByText(/owed to supplier:/i)).toBeInTheDocument()` AND `expect(screen.queryByText(/^outstanding:/i)).not.toBeInTheDocument()`. Same pattern for class assertions: `expect(badge).toHaveClass("text-secondary")` AND `expect(badge).not.toHaveClass("text-error")`. **Apply by default for any inversion test** — costs almost nothing, catches the class of regression that's hardest to spot during code review. Discovered Phase 4.

- **Never `source` / `set -a` `.env*` files that contain URL query strings.** Bash treats unescaped `&` as a background-job operator, silently truncating values like `DATABASE_URL=postgresql://...?pgbouncer=true&connection_limit=1` at the first `&` (and possibly storing an empty value if the rest of the line gets parsed as a separate command). The bug is invisible at the script layer — the variable IS set, just to the wrong value. Phase 4.5 hit this exact trap during the initial Vercel env-var upload: stored an empty `DATABASE_URL` in production for ~30 minutes before a debug endpoint surfaced `length: 0`. **Canonical pattern**: parse env files via Node, not via shell. Either `import { config } from 'dotenv'; config({ path: '.env.production.local' })`, or a one-shot `node -e` script that reads the file with `fs.readFileSync` and a regex. The repo's `scripts/walkthrough-prod.mjs` and `scripts/walkthrough-sales.mjs` show the read-with-regex pattern. **Detection trick** when you suspect env-var corruption against Vercel or any platform that encrypts at rest: temporarily expose presence + length (NOT values) via a token-gated debug route — see the (deleted) `src/app/api/debug-env/route.ts` git history for the shape. Discovered Phase 4.5.

- **Lazy Prisma client construction is required by Next.js 16 build-time page-data collection.** `src/lib/prisma.ts` wraps `PrismaClient` in a `Proxy` so connection-string read + adapter construction defer to first property access. **Do NOT** revert to eager construction (`const prisma = new PrismaClient(...)` at module load). Next.js 16 imports route modules during the "Collecting page data" build phase to extract metadata; eager construction would fire a throw on missing `DATABASE_URL` even when the env IS configured for runtime, because the build's env-resolution timing differs from runtime's. The throw still triggers correctly at runtime if the env is actually missing — it just no longer crashes the build. Phase 4.5 fixed this. See `docs/HANDOFF.md` § 3 and the file's header comment for the canonical lazy-client pattern.

- **Breaking enum migrations in non-interactive contexts.** `prisma migrate dev` requires interactive confirmation for breaking enum changes (removing a value, renaming, etc.) — the prompt "Are you sure you want to continue?" is unskippable and `--create-only` doesn't change this. In CI, agent shells, or any non-TTY context, the canonical workaround is: (1) update `prisma/schema.prisma`; (2) generate the SQL via `npx prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --script` to stdout; (3) hand-create the migration directory under `prisma/migrations/<timestamp>_<name>/` and paste the SQL into `migration.sql`; (4) apply with `npx prisma migrate deploy`, which is non-interactive and works against any environment. **Canonical example**: Phase 5 migration 2 (`20260515183353_role_remove_staff`) removed the `STAFF` enum value via this exact pattern — Prisma's diff produced the standard CREATE-new-type → ALTER-COLUMN → DROP-old-type sequence, written by hand into the migration file, applied to both dev and production via `migrate deploy`. Discovered Phase 5 migration.

- **Parameterised role tests via `describe.each(ROLE_MATRIX)`.** Every role-gated server action needs a parameterised role-access test that covers all four roles in a single `describe.each` block. The matrix is `[["ADMIN", boolean], ["PURCHASE_DEPT", boolean], ["LABOUR_MGMT", boolean], ["CASTING_PLATING_MGMT", boolean]]` where the boolean is `allowed`. Each case branches: allowed roles assert `result.ok === true` AND `prisma.<table>.<op>` was called once; denied roles assert the action throws `"Forbidden"` AND `prisma.<table>.<op>` was NOT called. **The DB-call assertion is the critical check** — without `expect(prisma.X.create).not.toHaveBeenCalled()`, a silent guard bypass (action proceeds past `requireRole` somehow) would pass the test. Pattern lives in every `*-actions.test.ts` file under the "Phase 5 RBAC" describe block. Mock setup: `vi.mocked(requireRole).mockResolvedValueOnce(sessionFor(role))` for allow; `vi.mocked(requireRole).mockRejectedValueOnce(new Error("Forbidden"))` for deny. Use `mockResolvedValueOnce` (not the default `mockResolvedValue` from `beforeEach`) so the per-test mock doesn't bleed into subsequent tests. Discovered Phase 5 tests.

- **`prisma.$transaction` in the shared Prisma mock.** The shared Prisma deep mock at `src/lib/__mocks__/prisma.ts` provides a default `$transaction(cb)` implementation that invokes the callback with the same deep-mocked client and returns its result. Tests for actions wrapped in `prisma.$transaction(async (tx) => …)` work without per-test setup — the existing `vi.mocked(prisma.<model>.<op>)` configurations fire through the tx path identically to a non-transaction action. **When to override**: tests that need to assert rollback behaviour ("if inner `tx.customer.create` throws, outer `tx.sale.create` is never called") use `vi.mocked(prisma.customer.create).mockRejectedValueOnce(...)` — the default `$transaction` impl propagates the throw and the per-test rejection rolls everything. Per-test `vi.mocked(prisma.$transaction).mockImplementationOnce(...)` is also available for finer control. Discovered Phase 6 build.

- **R2 (Cloudflare) bucket CORS must be set once via S3 API + Admin-scope token (Phase 8).** Browser → R2 PUT via presigned URL silently fails the CORS preflight if the bucket has no CORS policy — the user sees "Network error during upload" with no actionable detail in the browser console. **Resolution path**: generate a Cloudflare R2 API token with **Admin Read & Write** scope (the Object R&W token the app uses gets `AccessDenied` on `PutBucketCors`), apply CORS rules via `@aws-sdk/client-s3`'s `PutBucketCorsCommand` (allowed origins: prod alias + git-main alias + `https://*.vercel.app` + localhost dev origins; methods: GET/PUT/HEAD/DELETE; AllowedHeaders: `["*"]`; MaxAgeSeconds: 3600), then revert the prod env to an Object R&W token. CORS rules persist independent of the token that set them — one-time setup. **For new deployments / new R2 buckets**, the `scripts/_set-r2-cors.mjs` pattern (gitignored under `scripts/_*.mjs`) is the canonical implementation; preserve the script locally between rotations or generate fresh from the CORS JSON in `KNOWN_GAPS.md` decision lineage. Discovered Phase 8 walkthrough; full rationale in the Phase 8 decision-lineage block above.

- **Underscore-prefixed scratch scripts (`scripts/_*.mjs`) are gitignored by convention.** These are one-off operational tools (env-var sync, CORS provisioning, prod DB cleanup, etc.) that often embed file paths to `.env*.local` or perform admin-scope operations. They MUST NOT be committed. The convention: leading underscore → gitignored via the `scripts/_*.mjs` glob in `.gitignore` (added Phase 8 closeout). The walkthrough scripts (`scripts/walkthrough-*.mjs`) are committable — they're reusable verification tools that load creds from gitignored env files at runtime. Examples of acceptable scratch: `_set-r2-env.mjs` (Phase 8 — Vercel env sync), `_set-r2-cors.mjs` (Phase 8 — bucket CORS), `_cleanup-prod-pending-bills.mjs` (Phase 8 — one-off orphan cleanup), `_devtools-check.mjs` (ad-hoc browser-side debug), `_mirror-casting-to-plating.mjs` (Phase 9 — file mirror), `_mirror-casting-tests-to-plating.mjs` (Phase 9 — test mirror), `_cleanup-p9-walkthrough.mjs` (Phase 9 — walkthrough-marker scrub). Discovered Phase 8 closeout.

- **Decimal × BigInt arithmetic via `computeLineTotal` (Phase 9).** Pattern lives in `src/lib/weight-helpers.ts`. Any feature that multiplies a weight by a rate MUST use this helper — **never inline `weightKg.mul(ratePerKg.toString())` anywhere else.** Rounding is `ROUND_HALF_EVEN` (banker's rounding) — avoids systematic bias when summing many rounded transactions, distinguishes itself from `ROUND_HALF_UP` via the four canonical cases (`0.5→0`, `1.5→2`, `2.5→2`, `3.5→4`). The 8-case rounding sweep in `src/lib/weight-helpers.test.ts` pins all four distinguishing inputs plus four real-data examples (`1.875×35000=65625`, `0.001×50000=50`, etc.). If the rounding mode ever has to change, all 4 distinguishing cases will flip in a single test run — easy to catch. Companion serialisation rule: `weightKg` columns serialise as **strings** (e.g., `"2.500"`) at the action boundary so the 3-decimal-place gram-precision contract survives the JSON wire. Use `formatKg(s)` for display. Never `Number(decimal)` before display arithmetic. Discovered Phase 9.

- **Entity mirroring discipline (casting ↔ plating, Phase 9).** When two entity types share a structural mirror (Casting / Plating, and potentially future outsource categories), use the canonical mirror scripts `scripts/_mirror-casting-*-to-plating.mjs` (gitignored under `scripts/_*.mjs`). Always:
  - **(a)** Protect shared identifiers from substitution. The current protect list is `CastingPlatingVendor`, `castingPlatingVendor` (camelCase Prisma property — was a Phase 9 leak when missing), `casting_plating_vendors`, `CASTING_PLATING` (covers `CASTING_PLATING_MGMT`).
  - **(b)** Run `tsc --noEmit` after every mirror — TypeScript catches camelCase property leaks (`platingPlatingVendor`) and shape mismatches that grep won't.
  - **(c)** Grep for unexpected `casting` / `Casting` tokens in the mirrored side: `grep -rni "casting" "src/app/(app)/plating/" | grep -vE "CastingPlatingVendor|castingPlatingVendor|casting_plating_vendors|CASTING_PLATING_MGMT"` should produce zero matches.
  - **(d)** Re-apply any type-narrowing casts the mirror obliterates. Phase 9 hit this twice: a `castingPlatingVendor` camelCase leak in `plating/actions.ts` + `plating/page.tsx` (caught by tsc), and a `call.data.lineItems.create[0]` narrowing cast in `plating/actions.test.ts` that had to be re-applied post-mirror. The mirror script itself is dumb on purpose — it does string substitution; semantic gaps are caught by the verification steps. Discovered Phase 9.

- **Playwright walkthrough discipline (Phase 9).** Don't gate on intermediate UI states (a modal closing during transition, a form input disappearing momentarily). React state updates + `router.refresh()` are async and don't expose deterministic intermediate markers on prod latency. Instead, **wait for the final observable assertion** — e.g., the status chip flipping to "Partial" — and let everything in between just happen. Add diagnostic capture (screenshot + form-input count + dialog text snippet) inside a `try`/`catch` around the long-wait `waitForFunction` so a future timeout produces actionable output instead of just "timed out at line N." Phase 9 Step 12 timed out the first run on an intermediate "form disappeared" check; removing the intermediate gate + bumping the final timeout from 10s → 20s made it deterministic. Pair the discipline with marker-based cleanup: every entity created during a walkthrough carries `partyName LIKE '__phase{N}walk_%'`, vendor `name LIKE '__phase{N}walk_%'`, etc. — final cleanup step scrubs everything via that single LIKE filter regardless of which step actually created what. Discovered Phase 9 walkthrough.

- **Chain-ordering test pattern (Phase 10).** When a server-action chain has multiple ordered calls (e.g., bill replace: `softDeleteBill` → `prepareUpload` → R2 PUT → `confirmUpload` → `attach*ToEntity` for casting/plating), individual call-count assertions catch presence but miss order. Use a `callOrder: string[]` accumulator in the file's scope, push the call name from inside each mock's `mockImplementation`, then assert `expect(callOrder).toEqual([...exactSequence])` at the end. Catches regressions where a refactor changes the order even when individual calls all succeed — and `softDeleteBill` running AFTER `prepareUpload` (instead of before) would leak R2 objects and trip the `@unique billId` constraint. Canonical example: `bill-action-modal.test.tsx`'s `replace flow chain` describe block. Pattern applies anywhere a test needs to prove "these calls happen in this order" without per-call timing.

- **`userEvent.upload` bypass for testing client-side MIME rejection (Phase 10).** `<input type="file" accept="image/png,...">` paired with `userEvent.upload` respects the `accept=` filter and **silently drops** disallowed files — `onChange` never fires, no state update happens, and the test's rejection assertion passes vacuously (the rejection logic was never exercised). To test the JS-side `onPickFile` rejection logic, use `fireEvent.change(fileInput, { target: { files: [makeFile("doc.txt", "text/plain")] } })` to assign files directly, bypassing the picker's `accept=` filter. Canonical example: `bill-action-modal.test.tsx`'s `unsupported MIME types client-side` test. Apply anywhere a client-side file validation needs to be exercised against a file the browser-side picker would normally reject. Discovered Phase 10 testing.

- **`useFieldArray` React-key correctness.** When refactoring or testing a `useFieldArray`-based form (Phase 7 sale-form-modal / purchase-form-modal), **always use `field.id`** (RHF's internal field ID exposed via `fields.map((field) => …)`) as the React `key`, NEVER the array index. Using `idx` as `key` produces silent data swaps when a middle row is removed: type `"FIRST"` in row 1 and `"THIRD"` in row 3, then remove row 2 — with index keys, row 1 keeps "FIRST" but the new row 2 (formerly row 3) shows up under row 1's input element, scrambling the values. With `field.id` keys, React preserves the right `<input>` instance for each surviving line and the typed values follow the data. **Canonical assertion** in `sale-form-modal.test.tsx`: render the form, add lines to get 3 rows, type sentinel strings into rows 1 and 3, remove row 2, then assert (a) the first surviving row's input still holds the row-1 sentinel AND (b) the second surviving row's input holds the row-3 sentinel. If the assertion fails, the React key is index-based. Pattern established Phase 7.

- **Radix Dialog + outside-click in Playwright.** Radix Dialog (and similar portal-based components with click-outside-to-dismiss behaviour) interprets `page.locator("body").click()` as an outside-pointer event and closes the dialog via its `onPointerDownOutside` handler. Symptoms in a Playwright walkthrough: step 2 PASSes because its assertions run before the close animation completes; step 3 fails to find an in-dialog element with a "Timeout exceeded" error. **Fix**: blur via `page.keyboard.press("Tab")` inside the dialog — keyboard events don't fire the outside-pointer handler. Or move focus by clicking on another in-dialog element (e.g., a non-dismissing input). Same applies to any portal-based modal with `closeOnOutsideClick` (default for Radix Dialog, similar in Headless UI / Reach UI / shadcn's Sheet). Discovered Phase 7 walkthrough — the `walkthrough-p7-line-items.mjs` `fillLine` helper originally body-clicked to blur the rate input, silently closed the dialog, and step 3 ("+ Add line") couldn't find the button because the dialog was gone.

- **Mirror-script defences include data verification at walkthrough (Phase 10.5).** After running a mirror script (sales→purchases, casting→plating, etc.), the build checkpoint MUST include: (1) `npx tsc --noEmit` (catches type-level leaks like camelCase property names), (2) grep for source-entity string literals in target files (`grep -rni 'entityType="sale"' src/app/\(app\)/<target>/` — should be zero matches), (3) a walkthrough step that exercises the cross-system data path AND verifies the discriminator value in the DB (e.g., `SELECT "attachedToType" FROM bills WHERE "attachedToId" = '<new entity id>'`). **Phase 10.5 leaked `entityType="sale"` past tsc + initial grep** because the literal is a legal member of the discriminated union — only the walkthrough's explicit DB-side verification caught the data corruption (4 orphan SALE-typed Bill rows attached to purchase IDs). Adopt steps (1)–(3) by default for every mirror going forward. Onboarding companion to the decision-lineage entry above.

- **`ENTITY_MATRIX` pattern for shared-component tests (Phase 10.5).** Tests for components that ship across entity types (action modals, status chips, transaction-status helpers) use `describe.each(ENTITY_MATRIX)` where `ENTITY_MATRIX` is the readonly array of entity types the component supports. Adding a new entity type to the component (Phase 10.6 will add `casting` / `plating` to the action-modal matrices once their tables wire the dispatch) requires only an array addition — every existing test generates against the new entity automatically. Pattern lives in `payment-action-modal.test.tsx` (4 entries: sale/purchase/casting/plating), `return-action-modal.test.tsx` (2 entries: sale/purchase — Casting/Plating have no returns workflow), and `bill-action-modal.test.tsx` (4 entries via a `DISCRIMINATOR_MAP` of `[entityType, attachedToType]` pairs). Pair with the `callOrder: string[]` chain-ordering pattern (Phase 10 onboarding note above) when the test needs to prove the chain runs in a specific order across all entity types.

- **Mirror script defense-in-depth (consolidated lesson, Phase 10.6).** Three-layer defense against entity-mirroring bugs, all required because each catches a different bug class:
  - **Layer 1 — `tsc --noEmit`** catches type-level leaks: camelCase Prisma property mismatches (`platingPlatingVendor`), missing types, broken imports. Caught the missing `castingPlatingVendor` PRESERVE entry in Phase 10.6's first mirror run; 25 errors surfaced before the walkthrough.
  - **Layer 2 — Build-time grep** for source-entity string literals in target files: `grep -rE '"sale"|"SALE"|"customer"|"Customer"' src/app/\(app\)/<target>/`. Catches semantic-but-typed-correctly leaks where TypeScript accepts the string because it's a legal discriminated-union member. The Phase 10.5 incident (`entityType="sale"` in `purchases-table.tsx`, attached 4 orphan SALE-typed bills to purchase IDs) was caught only at walkthrough time before this layer existed.
  - **Layer 3 — Unit-level regression guards** in the target entity's table test pin the exact `entityType` value AND the FK-prop contract (whether `onAttach`/`onDetach` are present): `expect(modal.getAttribute("data-entity-type")).toBe("casting"); expect(modal.getAttribute("data-has-on-attach")).toBe("yes");`. Catches the leak at unit-test time, no walkthrough required. Lives in `casting-table.test.tsx` + `plating-table.test.tsx` + `purchases-table.test.tsx`.

  **All three layers required.** Each catches a different class:
  - Layer 1 misses string-literal leaks (TypeScript doesn't know they're wrong).
  - Layer 2 misses non-string-literal subtleties (a wrong import path that still types correctly).
  - Layer 3 misses anything the test author didn't think to assert.

  Discovered Phase 10.6.

- **Phase 11 split into 11.1 (foundation + transactional) and 11.2 (master data + forms + tests) (Phase 11.1).** Mobile-responsive layouts touch ~30 files plus ~70 new tests plus a Playwright mobile-viewport walkthrough plus three doc updates — too large for a single continuous session at the project's current scale. Phase 11.1 ships the **foundation infrastructure** (Sheet primitive, ResponsiveDialog wrapper, ResponsiveTable wrapper, `useIsMobile` hook, viewport meta tag) + the **transactional UX** (sidebar drawer + mobile header; mobile card view on Sales/Purchases/Casting/Plating tables; ResponsiveDialog applied to all 3 action modals + the 4 transactional detail modals; touch-target `min-h-[44px]` on mobile action buttons; dashboard grid verified responsive-out-of-the-box). All 990 existing tests continue to pass via a `window.matchMedia` stub added to `vitest.setup.ts` (default `matches: false` keeps non-mobile-aware tests on the desktop branch). Phase 11.2 will complete the migration: 4 master-data tables (Customers/Suppliers/Employees/Vendors) mobile cards, 4 master-data detail modals to ResponsiveDialog, 4 form pages mobile single-column layout (the line-item 5-column grid is the most invasive remaining work), Playwright mobile-viewport walkthrough, ~70 mobile-branch tests, CLAUDE.md §2/§6 + TESTING.md updates. **Rationale for the split**: the foundation in 11.1 is the riskiest part (one consistent abstraction across all responsive surfaces); the 11.2 work is mechanical fan-out using established patterns. Splitting limits regression surface per commit and keeps the diff reviewable. The transactional entities are the user's primary daily-driver surfaces, so mobile usability on Sales/Purchases/Casting/Plating lands first; master data + forms can ship in 11.2 without blocking the workflow.

- **Conditional render via `useIsMobile` over CSS-only dual-render (Phase 11.1).** ResponsiveTable and ResponsiveDialog use a `useIsMobile` hook to render exactly ONE branch (desktop OR mobile) rather than rendering both with CSS `display: none` on the inactive one. **Why not dual-render**: jsdom doesn't apply CSS layout, so `getByText` on row data would have matched both the hidden desktop table AND the mobile cards, breaking 22 existing tests. Switching to conditional render (with SSR-safe default = desktop) keeps the existing tests passing without modification. **Trade-off**: a one-paint-cycle flash of desktop content on initial mobile page load while React hydrates and `useEffect` fires. The flash is brief, only visible on the very first navigation to a page on mobile, and the alternative (dual-render + 22 test rewrites + a permanent `getByText` scope-to-container convention for every future test) is worse. The `useIsMobile` SSR default of `false` (desktop) is deliberate: server has no viewport, so defaulting to the "more common" branch (desktop is the platform the app was first built for) minimizes the flash for desktop users and concentrates it on mobile, where the flash is masked by the subsequent layout swap.

- **Window.matchMedia stub in `vitest.setup.ts` (Phase 11.1).** jsdom doesn't implement `window.matchMedia`. The `useIsMobile` hook calls it on mount and would throw `TypeError: window.matchMedia is not a function` in every test that mounts a component using ResponsiveTable / ResponsiveDialog / Sidebar. Phase 11.1 adds a default stub returning `matches: false` — keeps existing 990 tests on the desktop branch unchanged. **Phase 11.2 onboarding note**: tests that exercise the mobile branch override `window.matchMedia` in a `beforeEach` to return `matches: true` for the `(max-width: 767px)` query. See pending TESTING.md update.

- **FK-vs-discriminator bill attachment patterns (Phase 10.6).** Two entity categories handle bill attachment differently. **(a) Sales/Purchases use discriminator-only**: `bills.attachedToType` + `attachedToId` is the only link; no FK column on the entity row. Lookup goes through `getBillForEntity(attachedToType, attachedToId)` in `bills/actions.ts`. The 1:1 invariant ("at most one bill per entity") is enforced at the application layer via the replace flow's soft-delete-then-upload sequence, not via `@@unique`. **(b) Casting/Plating use FK + discriminator**: the entry row carries a `billId @unique` column AND `bills.attachedToType` + `attachedToId` is set. Lookup can go either direction (FK direct or discriminator); the page-data fetch uses the FK (`include: { bill: true }`) while the row-level `BillActionModal` uses the discriminator (so the modal stays entity-agnostic). The `BillActionModal` handles both via optional `onAttach` / `onDetach` props — when present, the modal calls them at the right points in the chain (`onDetach` BEFORE soft-delete, `onAttach` AFTER `confirmUpload`); when absent, those steps are skipped. **Adding a new entity type with bill support** — choose category based on the schema-level relationship: if the entity's relationship to bills is exactly-one (enforce 1:1 at the DB layer), use FK; if many-to-one with no schema-level cardinality (different kinds of bills attached over time), use discriminator-only. Sales/Purchases are 1:1 in practice but not at the schema level (the app enforces it); Casting/Plating are 1:1 at the schema level via the `@unique` constraint. Both work for the current single-bill-per-entity workflow; the schema choice reflects whether you want the DB to be the source of truth on cardinality.

- **Mobile responsive verification requires actual viewport-level visual checks (Phase 11.2).** Unit tests with `mockMobileViewport()` from `src/test-utils/viewport.ts` verify branch logic (which JSX renders given `useIsMobile()` returns true) and **regression-check responsive class strings** (`md:contents`, `hidden md:grid`, `h-11 md:h-10`, etc. exist on the right elements). They do NOT verify visual correctness — JSDOM doesn't evaluate CSS media queries or compute layout boxes. **The Phase 11.1 horizontal-scroll regression** (commit `8c7b06c`) and **the Phase 11.2 form-page padding issues** were both caught only by real-phone testing. **Pattern**: walkthrough on real phone OR DevTools at 380–390px width BEFORE claiming a phase is mobile-ready. Build checkpoint reports must explicitly state whether viewport-level checks happened in the session — if they didn't, the walkthrough phase MUST do them. Don't ship a "mobile" phase based on tsc + unit tests alone.

- **`src/test-utils/viewport.ts` helper for mobile-viewport tests (Phase 11.2).** Import `mockMobileViewport()` or `mockDesktopViewport()` and call inside `beforeEach` of a `describe('mobile viewport', …)` block. The helpers overwrite `window.matchMedia` to return `matches: true` (or `false`) for any query — which flips `useIsMobile()` to the mobile branch on the next render. Existing tests stay unaffected because the global `vitest.setup.ts` stub defaults to `matches: false`. Use a separate `describe` block per mobile-mode test rather than mixing modes in the same block — the global stub is reset between test files but not between tests within a file, so a `mockMobileViewport()` call leaks to subsequent tests in the same `describe` unless explicitly reset. Pattern documented in `docs/TESTING.md` mobile-viewport subsection.

- **Visual restyle to light theme — single-file remap via `globals.css` @theme (Phase 11.5).** Phase 1's "techno-artisanal" dark navy palette was retired in favour of a cream/slate/gold **light theme** in Phase 11.5. The codebase architecture made this a trivially-scoped change: 290+ semantic-token usage sites (`bg-surface`, `text-on-surface`, `border-outline-variant`, etc.) across 46 files all resolve through `@theme` CSS variables, so remapping the `--color-*` token values in `src/app/globals.css` flipped the entire codebase atomically. Five files modified total: `globals.css` (palette remap), `transaction-status-chip.tsx` (new tinted-pill scheme), `save-dropdown.tsx` (drop `shadow-lg` — flat-and-sharp), `bill-preview.tsx` (`bg-black` → `bg-surface-container-highest` — cream-themed image backdrop), and `design-system/page.tsx` (displayed hex values updated). **Rejected alternative**: touch every component to use direct `bg-[#hex]` literals per the original brief's "no semantic CSS variable tokens" line — would have been ~5,000 LOC for zero functional benefit and worse maintainability. The semantic-token architecture remains; the values changed. **Hardcoded** in the sense the user intended: hex values appear in **one place** (`globals.css` @theme block), no theme switcher, no light/dark variant CSS. All 1057 existing tests passed unchanged — behavior is preserved; only visual surface changed. Discovered Phase 11.5 planning + audit confirmed during build checkpoint.

- **Flat-and-sharp components reaffirmed (Phase 11.5).** Every `--radius-*` token in `globals.css` is `0px` (except `--radius-full: 9999px` reserved for status-chip dots). Drop shadows are forbidden — Phase 11.5 removed the lone `shadow-lg` on `save-dropdown.tsx`'s dropdown menu and replaced visual definition with `border border-outline` (cream-tan 1px border on the white menu surface). Tailwind v4 `rounded-xl` / `rounded-lg` classes that remain in `dialog.tsx` and `button.tsx` resolve to 0px through the radius tokens — they're harmless. **Onboarding rule**: don't add `rounded-` literals or `shadow-` classes to new components. Use defined 1px borders + tonal layering (`bg-surface` ↔ `bg-surface-container`) for visual hierarchy.

- **Status chip palette pinned at the chip component (Phase 11.5).** `src/components/transaction-status-chip.tsx` is the **only** file in the codebase that uses direct `bg-[#hex]` / `text-[#hex]` literal Tailwind classes. The four chip colors (`#8a8e9a` pending / `#c9844a` partial / `#5e8c4f` completed / `#5a7ba6` refund_due) are pinned at the component because their semantic meaning is "status colors" — distinct from any of the four semantic colour roles (primary/secondary/tertiary/error). Mapping them through `--color-primary` etc. would mean primary's identity drifts whenever the chip palette needs adjustment, or vice versa. Pinning at the chip keeps both palettes independent. Pattern applies to any future "fixed-semantics colors" that should NOT participate in the global theme remap.

- **Deferred — semantic-token-driven dark mode (Phase 11.5).** The Phase 11.5 light theme remaps hardcoded hex values in `globals.css` directly. A future dark-mode toggle would refactor those values into `:root { --color-surface: ... }` for light + `.dark { --color-surface: ... }` (or similar) for dark, then add a runtime theme switcher. **Effort estimate**: 2–3 hours to extract token values into per-theme blocks + add a theme-toggle button + persist preference in localStorage / cookie. Components require no changes — every consumer is already token-aware. Revisit if dark mode is requested.

- **Deferred — typography refresh (Phase 11.5).** Phase 11.5 kept Geist + Inter (the Phase 1 typography pairing) unchanged. The "techno-artisanal" aesthetic feels naturally cohesive with Geist/Inter, but a future typography refresh (e.g., Hanken Grotesk for headings, JetBrains Mono for tabular data) could deepen the editorial feel. **Effort estimate**: 30 min for the font swap via `next/font/google`, plus a walkthrough to verify legibility at small sizes. Revisit if visual identity needs further differentiation.

- **Photos on Purchases via the existing `Bill` table — discriminator-only reuse (Phase 12a).** Phase 12a adds gallery photo attachments to Purchases. The locked decision was: extend `ATTACHED_TO_TYPES` with a new `PURCHASE_PHOTO` value rather than (a) creating a parallel `Attachment` table, or (b) renaming `Bill → Attachment`. **Why**: the Bill table already encapsulates everything needed — R2 key, MIME, size, status state machine, soft-delete, role-aware access via `bill-access.ts`. Adding a new discriminator extends `ATTACHED_TO_TYPES` (one line), `ROLE_MATRIX` (one line), and the photo-specific MIME allowlist (`PHOTO_MIME_TYPES`, excludes PDF) — zero schema changes, zero migration. **Trade-off accepted**: the table name "Bill" is now semantically misleading (it stores invoice receipts AND visual photos). Logged below as a separate deferred rename. **Rejected alternative — new `Attachment` table**: would have duplicated 80% of the Bill schema (R2 key, status state, soft-delete, uploader FK, etc.) just to get a more accurate name; the polymorphism is what the discriminator pattern already solves. Discovered Phase 12a planning.

- **Party unification via role flags, not enum array (Phase 17a).** Customer / Supplier / CastingPlatingVendor merged into a single `Party` table with four boolean flags (`isCustomer`, `isSupplier`, `isCastingVendor`, `isPlatingVendor`). **Rejected alternative — Postgres enum array column** (`roles ARRAY OF Role`): would have required `roles @> ARRAY['CUSTOMER']` containment queries for every list-by-role page, which Prisma's typed query API expresses awkwardly; the boolean-flag shape gives clean `where: { isCustomer: true }` filtering, indexable per role, and zero raw-SQL escape hatches. The flag count is bounded by the workshop's actual role taxonomy (~4–6 max even with future "polishing vendor" / "welding vendor" additions); the enum-array's upside (extensibility without schema change) doesn't outweigh the query ergonomic loss at this scale. **Casting/Plating flags split intentionally**, even though the Phase 9 `CastingPlatingVendor` model didn't distinguish — real workshops often do one but not the other, and the migration sets both flags on existing rows (matching the prior model's semantics) while leaving future walk-ins to set only the form-context flag. **Phone is globally unique** across Party (`@unique` on the column) so the migration consolidates rows sharing a phone into a single Party with all relevant flags set, and walk-in auto-promotion uses phone as the single identity anchor across all four forms. Discovered Phase 17a planning.

- **Walk-in auto-promotion implicit role from form context (Phase 17a).** The four transactional forms (`/sales/new`, `/purchases/new`, `/casting/new`, `/plating/new`) each set a different role flag during walk-in auto-promotion: `isCustomer` on sales, `isSupplier` on purchases, `isCastingVendor` on casting, `isPlatingVendor` on plating. The walk-in input form has no "Is this a customer / supplier / vendor?" question — the form's URL implies the role. **Phone collision behavior**: if the Party already exists at the typed phone, the existing row gets the new flag added (rather than duplicating). The action looks up by phone first; if found and the appropriate role flag is missing, flips the flag inside the transaction. If the user explicitly picks an existing Party from the picker dropdown that doesn't have the matching role flag, the action ALSO flips the flag (e.g., a Party first created as a Supplier picked from the Sales form gets `isCustomer: true` added). All inside `prisma.$transaction`. **Rejected alternative — require explicit role selection on walk-in form**: would add a click for the common case (`/sales/new` always means customer); the form context is enough. **Rejected alternative — never flip flags on existing parties**: would force the user to manually edit a Party to add a role, breaking the "type the phone, it just works" UX. Discovered Phase 17a planning.

- **Master data route names preserved (Phase 17a).** `/customers`, `/suppliers`, `/vendors` remain after the Party unification — each page queries Party filtered by the matching role flag. The user-facing UI labels (page titles "Customers" / "Suppliers" / "Vendors", picker placeholders "Customer name or walk-in" etc.) preserved verbatim. **Rejected alternative — single `/parties` unified page**: muscle memory ("/customers shows my customers") was the dominant constraint; users don't think about the data unification, they think about the role. If a unified party admin view becomes useful later (e.g., a Phase 17b party-rollup screen for the owner), it can be added at `/parties` without disturbing the existing routes. **Type imports aliased** (`import type { Party as Customer } from "@/generated/prisma"`) so existing UI code keeps its descriptive local type names. The Party type structurally satisfies all three legacy types (it has every field they had, plus role flags + audit columns). Discovered Phase 17a planning.

- **Party-rollup as the primary payables/receivables view (Phase 17b).** `/payables` and `/receivables` default to a one-row-per-party rollup with the summed outstanding amount across all in-scope transactions; the per-party detail page (`/payables/[partyId]`) is one click away when the user wants to see the underlying transactions. **Rejected alternative — transaction-list-first view** ("unpaid Bill #142, unpaid Bill #156, etc."): matches the bookkeeping mental model but not the user's. The workshop user thinks "I owe Ramesh ₹30,000" not "I owe Ramesh ₹15k on Bill #142 + ₹15k on Bill #156." Party-first matches that mental model; one extra click surfaces the bill-level breakdown when needed (e.g., to decide which to settle first). Discovered Phase 17b planning.

- **Bulk party-level payment with user-picked allocation (Phase 17b).** `<PartyPaymentModal>` lists all unpaid transactions for the chosen party and lets the user check the ones to pay + edit per-row amounts; the sum is the actual payment value. **Rejected alternative — FIFO auto-allocate**: typing "₹20,000" and having the system auto-apply to oldest-first transactions is faster but loses transparency. The accountant needs to see "which bill am I paying right now" to reconcile with the supplier's invoice; auto-allocation forces a follow-up "what just got paid?" UX step. User-picked is the bookkeeping-correct shape. The bulk action `createPartyPayment` runs all `*Payment.create` calls inside one `prisma.$transaction` — if any single create fails (rare, but possible on FK violation or DB error), all roll back. Per-allocation validation rejects over-allocation with `errors.allocations[i]` so the modal can surface the error on the failing row rather than a generic banner. Discovered Phase 17b planning.

- **Role-scoped payables, ADMIN-only receivables (Phase 17b).** `/payables` access matrix matches the Phase 5 RBAC principle (each role sees the slice of data its workflow touches): ADMIN sees all sources unified, PURCHASE_DEPT sees only purchase payables, CASTING_PLATING_MGMT sees only casting+plating payables, LABOUR_MGMT has no access. `/receivables` is ADMIN-only because the customer-facing book is the owner's concern, not departmental. Defense-in-depth at three layers: (a) proxy `ROUTE_ROLES` gate at URL level; (b) page server-component re-checks via `canViewPayables(role, scope)` / `canViewReceivables(role)` and redirects to /dashboard if denied (catches deep links + URL hacks that bypass the proxy via internal navigation); (c) `createPartyPayment` server action gates by role-intersection across all allocation entity types (a mixed sale+purchase bulk requires ADMIN role since that's the only role in both `SALE`'s and `PURCHASE`'s allowed sets). Discovered Phase 17b planning.

- **Photos attach to parent Purchase, not per-line-item (Phase 12a).** Workshop workflow is "photograph the receipt + the goods collectively" — there's no per-item lens. Per-line-item photos would have added a `PURCHASE_LINE_ITEM_PHOTO` discriminator + per-line gallery + per-line photo count badge, all for no captured workflow value. If a use case emerges where one line item needs distinct visual evidence (e.g., disputed quality on a specific SKU), revisit with a `PURCHASE_LINE_ITEM_PHOTO` discriminator + add a gallery section to a line-item-edit modal. Discovered Phase 12a planning.

- **Multi-photo gallery (no hard limit) — R2 capacity is the practical ceiling (Phase 12a).** The schema imposes no per-purchase photo limit. R2's free-tier 10GB storage at 10MB per photo ≈ 1000 photos before any cost; the workshop's realistic upper bound is 5-10 photos per purchase × ~100 purchases/month × 12 months ≈ 12000 photos in year one (still under cost threshold given typical photo sizes <2MB). **If we ever cross the free tier**: either compress/resize on upload (server-side or client-side) or enforce a per-purchase soft limit (e.g., "Max 10 photos per purchase" client-side warning + server hard limit). Discovered Phase 12a planning.

- **Create-mode photos use a collect-then-batch pattern; edit-mode uses immediate uploads (Phase 12a).** In **create mode**, the form holds `pendingPhotos: File[]` locally + renders thumbnails via `URL.createObjectURL`. On save, the chain runs sequentially per photo: `prepareUpload(PURCHASE_PHOTO, savedPurchaseId) → R2 PUT → confirmUpload`. Failures of individual photos surface in a banner but don't block the form (the purchase row stays saved). In **edit mode**, `<PhotoGallery mode="edit">` self-loads + uploads/deletes immediately — no pending state on the form. **Why hybrid**: create mode needs the purchase id to attach to (chicken-and-egg), so photos must collect locally + commit after `createPurchase` returns. Edit mode already has an id, so direct uploads are simpler and give immediate feedback. **Rejected alternative — require save first then enable photos**: would have forced a two-step UX for new purchases ("save, then edit, then add photos") that breaks the "everything in one form save" pattern established by Phase 10's bill-in-form retrofit. Discovered Phase 12a planning.

- ~~**Deferred — rename `Bill` table to `Attachment` (Phase 12a tech debt).**~~ *(resolved 2026-05-19 — see resolved section below)*

- **Phase 12a — Purchase photos via gallery + lightbox. Resolved on 2026-05-18.** Adds `PURCHASE_PHOTO` discriminator to the existing Bill table, new `getPhotosForEntity` action, `<PhotoGallery>` + `<PhotoLightbox>` reusable components, photo section in Purchase form (create-mode batch + edit-mode live), photo gallery in detail modal (gated on `photoCount > 0`), photo-count badge on purchases-table rows. +39 tests across 7 files (1057 → 1096). All schema/access changes are additive — zero migration required. Walkthrough verified all 10 steps including keyboard nav, mobile sheet lightbox, delete + re-add roundtrip.

- **Phase 11.5 — Visual restyle to "techno-artisanal LIGHT" theme. Resolved on 2026-05-18.** Five files modified: globals.css (palette remap), transaction-status-chip.tsx (tinted pills), save-dropdown.tsx (drop shadow), bill-preview.tsx (cream image backdrop), design-system/page.tsx (display values). Headless Playwright walkthrough verified all 12 steps (8 desktop + 4 mobile) with palette-pinning assertions (body bg cream, card bg white, save button gold `#c9a14a`, pending chip slate `#8a8e9a`). 1057 / 1057 existing tests pass — behavior preserved. `npx tsc --noEmit` clean, `npm run build` clean (20 routes). `.next` cache had to be purged after editing globals.css to dislodge stale compiled CSS from the prior `npm run build` — clean-restart of the dev server is required after `@theme` value changes.
